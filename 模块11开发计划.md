# 模块11开发计划：分钟级数据（观察口径）

> 起因：用户发现 Yahoo Finance 有更精确的 tick/分钟数据，询问能否把日线升级到分钟级。
> 结论：历史分钟无法回填（源端窗口硬限制），采用「5m 前向采集 + 60m 回填近2年」双粒度补强。
> 定位：**纯观察口径新增能力**——不触碰冻结回测、不改任何信号/策略口径（冻结协议合规：数据扩展允许）。

## 一、探测结论（2026-09-10 实测冻结）

| 探测项 | 结果 | 决策 |
|--------|------|------|
| 各粒度回溯上限（AAPL 递进探测，超限 HTTP 422） | 1m 仅 7~8 天；5m/15m/30m 仅 60 天；60m 730 天 | 采集粒度锁定 5m+60m；1m 窗口太短无价值，15m/30m 与 5m 同窗不增量 |
| 池内 5 股可用性（1m/5m/60m） | 全部可用；A股 ~48根/日(5m)，港股 ~59根/日，美股 ~56根/日 | 全池直接开采 |
| 分钟响应是否含 adjclose | **不含**（indicators 仅 quote，探针 2026-09-10） | 落库口径=未复权；与 daily_quotes 的 qfq 对齐留到分析层（同日 raw/qfq 比值换算） |
| 单次请求上限 | 60m 730天 3498根一次拉满；5m 60天 3277根一次拉满 | 无需分块，每股每粒度 1 请求 |
| 时区 | meta.exchangeTimezoneName 可靠（Asia/Shanghai / Asia/Hong_Kong / America/New_York） | trade_date 取交易所本地日期（美股盘后K线归属纽约日期，不用北京日期） |
| 深度异常股 | 02513 60m 仅自 2026-01-08；SNDK 60m 自 2025-02-24（分拆上市日） | 源端深度事实，非缺陷（同模块10 #6 教训：先验第二源再定性） |

**为什么不能"日线升级成分钟级"**：库内日线自 2020 年起（6年+），1m 只有 7 天、5m 只有 60 天——历史分钟数据 Yahoo 根本不给（付费源 Polygon/IQFeed 才有）；且 14 条信号规则全部基于日线指标、回测引擎按日推进，改分钟级=重写系统+违反冻结协议。

## 二、核心设计决策（2026-09-10 冻结）

### 2.1 表结构与落库语义
- 新表 `intraday_quotes`，`UNIQUE(code, interval, ts)` + `INSERT OR REPLACE`
- **为何 REPLACE 而非 IGNORE**（与日线兜底的 IGNORE 相反）：Yahoo 是该表**唯一源**，不存在"兜底源污染主源"问题；当日未收盘的部分K线、迟到修正需要覆盖自愈；重跑幂等（同数据→同行数）
- 字段：code/market/interval('5m'/'60m')/ts(UTC epoch)/dt_local(交易所本地时区)/trade_date(本地日期)/OHLCV/data_source
- 口径：**未复权**。与 daily_quotes(qfq) 的对齐在分析层做（取同日 Yahoo raw close 与 adjclose 的比值缩放当日分钟序列）
- 查询索引 `(code, interval, trade_date)`——分析入口按 股票×粒度×交易日 取数

### 2.2 采集策略：双粒度分工
- **60m**：唯一能回填的粒度，730 天窗口 → 回填近 2 年 + 每日增量
- **5m**：60 天容错窗 → 前向积累为主；窗口宽意味着管道中断数周也能补回
- 每日管道增量窗口 7 天（容错一周缺口），`INTRADAY_REFRESH_DAYS=7`
- 限速：请求间 sleep 1.0s（探测 1.2s 无封禁）

### 2.3 入池引导链 v1.2（8步→9步）
- 新增第⑧步 `_onboard_seed_intraday`（overlay_seed 之后、join_event 之前）：新股入池时分钟数据**全窗口一次拉满**（5m 60天+60m 730天）
- 与模块10 #5 教训同源：管道日常步只补 7 天，新股历史分钟若不在引导链补齐将永久缺失
- 非数字代码（如测试桩 TESTHK）港股符号映射返回 None → 优雅跳过，引导链不炸

### 2.4 管道 10 步
- `run_daily_pipeline` 在 quotes 后新增 `step('intraday', ...)`（行情族相邻）
- 单步失败不阻断（管道既有隔离语义），分钟数据缺一天不产生任何下游影响（无依赖方）

### 2.5 边界诚实披露
- 60m 历史仅 2 年（2024-09 起）：更早的信号日（2021~2024.09 回放段）无分钟路径可查
- 02513 60m 自 2026-01-08、SNDK 60m 自 2025-02-24：源端深度，非回填缺陷
- 观察口径：任何基于分钟数据的研究结论只进观察/前瞻裁决，不回改冻结回测

## 三、交付物清单

| # | 交付物 | 说明 |
|---|--------|------|
| 1 | quant_data.py 模块11节 | 常量(INTRADAY_INTERVALS/SRC_LIMITS/REFRESH_DAYS/BACKFILL_DAYS)、create_intraday_table、fetch_yahoo_intraday(窗口截断+时区换算)、save_intraday_quotes(幂等+REPLACE+NaN→NULL)、collect_intraday_quotes(全池/单股)、backfill_intraday_quotes、_onboard_seed_intraday(引导链⑧)、get_intraday_status(读路径无DDL)、get_intraday_bars(分析入口) |
| 2 | init_db | intraday_quotes 表 + idx_intraday_lookup 索引 |
| 3 | daily_pipeline.py | 步骤注释更新 10 步 |
| 4 | dashboard.py Tab1 | 「分钟级数据」区：覆盖视图(每股×粒度 行数/起止) + 回填按钮(幂等) |
| 5 | test_module11.py | 39 项检查（临时库副本 + live 冒烟 + 时区判别 + 幂等/REPLACE/NaN + 注册检查 + 读路径无DDL） |
| 6 | 数据 | 首次回填 28,829 根（5股×2粒度；60m 至 2024-09-10，5m 至 2026-07-13） |

## 四、期望演进记录（非错误，防呆设计）

### test_module10「管道 9 步」→ 10 步
- 模块11 在 run_daily_pipeline 加入 intraday 步骤，源码 `step('` 计数 9→10；test_module10 第404行期望同步更新（同模块10 #3 期望漂移链规则：结构性变更先改期望再跑回归）

### test_module7「报告为模块10后9步」→ 兼容 9/10 步
- 该检查读 forward_meta.pipeline_last_run（上次真实运行的报告）。历史报告 9 步、模块11 后 10 步，改为 `len in (9, 10)`；旧报告(6/8步)仍 SKIP 不判失败

## 五、验证记录（2026-09-10）

- test_module11: **39/39 通过**（含纽约时区判别：21 个交易日首末根时段 0 异常；窗口截断 span=58.3 天；采集级幂等重跑零新增）
- 全量回归: m3 ✓ / m5 31 / m6 64 / m7 45 / m8 37 / m9 62 / m10 67 / d3 32 —— **全绿，零期望漂移**（模块11 不触碰信号/回测，d3 无变化符合预期）
- 真实库回填: 28,829 根入库，覆盖视图见 Tab1
- Streamlit 重启生效（模块加载一次约定）

## 六、后续可能方向（未承诺，仅记录）

- 分析层：信号日/入场日分钟路径研究（get_intraday_bars 已备好入口）；qfq 对齐换算工具
- 若 5m 前向积累满 1 年，可评估执行时点优化（如信号日次日开盘 vs 收盘入场的路径差异）——任何结论走观察→前向裁决，不回改冻结回测
