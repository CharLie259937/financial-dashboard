# 模块9开发计划：宏观×策略融合（FDR 裁决 + Overlay 风险开关）

> 优先级 P1（前向等待期计划第三项）。依据：模块8 扩容后宏观研究已有 2023-01 起 16280 事件 / 1602 组结果，但无 FDR 校正下名义显著组（146 组 p<0.05）假阳性预期 ~80 组、头部多为小样本伪影（n=2 贸易账 / n=5 核心PPI）——先裁决出可信家族清单，才能进入策略层。
> **目标状态**：FDR 预注册裁决 → overlay 配置冻结 → D1 引擎封锁日开关（S2M/S1aM）→ 扩展回测批次（机制对照）→ 前向并行跟踪 → UI 三处呈现。

## 一、现状基线（2026-09-06 实施前冻结）

| 项 | 现状 |
|----|------|
| macro_study_results | 1602 组（2026-09-05 00:18 重算，事件 2023-01-02~2026-09-03，star=2，6 窗口，方向拆分） |
| 名义显著 | p<0.05 共 146 组、p<0.01 共 64 组（**无任何校正，不可直接引用**） |
| D1 引擎 | `_run_strategy_bt(data, triggers, hold, max_pos, fee)` 纯函数，无封锁概念 |
| BT_STRATEGIES | S1a~S1d/S1e/S2 共 6 个（冻结于 2026-08-29），前向/回测均迭代此字典 |
| 每日管道 | quotes→benchmark→indicators→signals→wide_table→forward→freshness，**不含宏观采集**（手动） |
| 宏观源特性 | `collect_macro_events` 对最近 3 天强制重采（MACRO_RECENT_REFRESH=3），公布值可能后填 → 方向（高于/低于预期）可能迟到翻转 |

## 二、预注册裁决规则（2026-09-06 看结果前冻结，一经写入不得依结果修改）

### 2.1 FDR 裁决（主裁决）
1. **方法**：Benjamini-Hochberg step-up，**q = 0.05，按窗口分层**（6 个窗口 = 6 个独立检验族：`[0,+0]` `[0,+1]` `[0,+3]` `[0,+5]` `[0,+10]` `[-5,+10]`）
2. **分层内**：p 值升序，rank i 的临界值 = q·i/m；存活 = 满足 pᵢ ≤ q·i/m 的最大 rank 及其以上全部
3. **Overlay 资格**（四条件同时满足）：
   - ① BH 存活（该组在其窗口层内通过 q=0.05）
   - ② n ≥ 30（排除小样本伪影）
   - ③ car_mean < 0（**只减不加**：负 CAR 才封锁开仓，正 CAR 组只记录不行动）
   - ④ 窗口形状为 `[0,+N]` 且 N ≥ 1（`[0,+0]` 无可封锁日、`[-5,+10]` 含事件前窗口，均只参与 FDR 不参与 overlay）
4. **防退化护栏**：单组封锁日覆盖率（该组封锁日 ÷ 该股研究窗口交易日数）> 50% 则不入选（overlay 是风险开关不是停机开关，防止 S*M 退化为永不交易）
5. **窗口选择**：同组多窗口合格时取**最短 N**（最小干预，与"只做减法"同源）
6. **主裁决对象**：当前全窗口研究（2023-01 ~ 2026-09-03）的 1602 组

### 2.2 诊断性对照（非门槛，结果无论如何主裁决不变）
- 用知识截止（2026-05-01）前子样本重跑研究 + 同规则 FDR，报告两组 overlay 入选清单的重合度
- 目的：暴露选择泄漏的脆弱性；若主清单在子样本中零存活，结论中必须显著标注
- **选择泄漏披露**：研究窗口含知识截止后数据 → overlay 家族选择用了 oos 期信息 → **overlay 回测的 full/oos 段均为机制对照，唯一干净证据 = forward test**

### 2.3 Overlay 机制（预注册）
1. **策略**：`S2M`（= S2 + 宏观封锁）、`S1aM`（= S1a + 宏观封锁）——触发/持有期/槽位/费率与原版完全一致，唯一差异 = 封锁日不开新仓；新增策略不改原 6 策略任何行为
2. **股票-指数映射**（按市场推导）：港股→HSI、A股→sh000300、美股→.INX；入选组按 index_code 匹配股票
3. **封锁窗口**：事件用 `macro_event_trading_day`（股票自身交易日历）映射到交易日 t 后，封锁该股 **t+1 至 t+N** 的开仓日（N = 该组最短合格窗口长度；不含 t 当日，与全框架 T+1 成交惯例一致）
4. **只封锁新开仓**：已持仓不受影响、不提前平仓、不加新信号、不改费率
5. **多组取并集**：任一入选组封锁即封锁
6. **日决策台账（macro_overlay_days）**：(code, trade_date, group_id) 唯一键追加式，**决策一经写入永不改写**——宏观源对最近 3 天强制重采、公布值后填会迟到翻转方向，台账保证前向重放确定性（杜绝 NAV 守卫误报）
7. **台账刷新守卫**：管道刷新只追加 trade_date > 完整性截断日（stock_cap）的决策——已入库净值覆盖的日期永不回补封锁（迟到信息只作用于未来，见 2.4）
8. **配置冻结**：入选组清单 + 全部参数写入 macro_overlay_meta，交付后不再依回测结果修改；宏观研究重跑**不**自动更新 overlay（需新的预注册决策）

### 2.4 迟到信息语义（预注册示例）
事件日 D 公布值空白 → 当日无方向 → 不封锁；D+2 公布值后填且方向命中入选组 → 自 D+3 起的未入库日期开始封锁（D+1/D+2 已按未封锁入库，不改写）。这是台账守卫的必然结果，属诚实记录而非缺陷。

### 2.5 前向裁决（预注册，写入 forward_meta 独立键）
- S2M/S1aM 为并行观察；各自**完成交易 ≥ 20 笔**后与原版对比（原版 = S2/S1a 同窗口前向值）：
  - **淘汰**：excess_vs_B1(S*M) < excess_vs_B1(S*)——overlay 拖累收益
  - **保留（转正）**：excess_vs_B1(S*M) ≥ excess_vs_B1(S*) 且 max_drawdown(S*M) 收窄（大于原版，即更不负）
  - **其余**：继续观察
- 裁决规则与回测冻结协议同源，不得依前向结果修改

## 三、表结构变更（全部 CREATE IF NOT EXISTS，不动既有表数据）

```sql
-- FDR 裁决结果(每次裁决全量重建)
CREATE TABLE IF NOT EXISTS macro_fdr_results(
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    run_ts TEXT NOT NULL, window TEXT NOT NULL,
    region TEXT, family TEXT, index_code TEXT, direction TEXT,
    n INTEGER, car_mean REAL, t_stat REAL, p_value REAL,
    p_rank INTEGER, m_family INTEGER, bh_critical REAL, q_value REAL,
    survive INTEGER, overlay_eligible INTEGER, eligible_reason TEXT,
    run_date TEXT
);
CREATE TABLE IF NOT EXISTS macro_fdr_meta(key TEXT PRIMARY KEY, value TEXT);
-- keys: protocol(预注册规则+frozen_at) / last_run(计数) / diagnostic_cutoff(对照结果)

-- Overlay 封锁日台账(追加式, 永不改写)
CREATE TABLE IF NOT EXISTS macro_overlay_days(
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    code TEXT NOT NULL, trade_date TEXT NOT NULL, group_id TEXT NOT NULL,
    window_days INTEGER, run_ts TEXT, run_date TEXT,
    UNIQUE(code, trade_date, group_id)
);
CREATE TABLE IF NOT EXISTS macro_overlay_meta(key TEXT PRIMARY KEY, value TEXT);
-- keys: config(冻结的入选组+N+参数) / adjudication(前向裁决规则)

-- 既有表追加列(幂等迁移)
ALTER TABLE backtest_runs ADD COLUMN n_macro_blocked INTEGER;  -- 旧批次 NULL
```

## 四、代码改造清单

| # | 位置 | 改造 |
|---|------|------|
| 1 | `_run_strategy_bt` | 加 `blocked_days=None`（{code: set(dates)}）：开仓日命中 → `macro_blocked` 计数并放弃；返回值加 `macro_blocked`；默认 None 行为不变（向后兼容） |
| 2 | `BT_STRATEGIES` | 追加 S2M/S1aM（`overlay: True`，match/hold 复制原版） |
| 3 | `run_backtest` | spec.get('overlay') 且未传入 blocked_days → 自动从台账加载 |
| 4 | `_load_overlay_blocked(data)` | 台账 → {code: set(dates)}，池过滤 |
| 5 | `run_macro_fdr(q, min_n, max_cov)` | BH 分层裁决 + 诊断对照（run_macro_study 加 end_date 参数）+ 入库 + 冻结 overlay 配置 |
| 6 | `refresh_overlay_days(min_date)` | 台账追加（守卫：只插 trade_date > min_date；交付种子 min_date=None） |
| 7 | `run_d2_backtests` | 透传 blocked_days；runs 表写 n_macro_blocked |
| 8 | `run_forward_step` | 同上；status_strategies 加 macro_blocked |
| 9 | `run_daily_pipeline` | 插入 `macro`（采集最近 3 天，幂等）+ `overlay_days`（台账追加, min_date=stock_cap）两步，置于 forward 前 |
| 10 | `evaluate_overlay()` | 前向裁决读数（对比原版，规则预注册） |
| 11 | `dashboard.py` | Tab 8 加「FDR 裁决与 Overlay 配置」区；Tab 7 加 S*M 对照块 + 策略配色；Tab 9 加 overlay 裁决面板 |
| 12 | `test_module9.py` | BH 数学正确性/资格四条件/台账幂等与守卫/封锁日只拒新仓/S*M ⊆ S* 交易集/批次与前向集成/确定性重放 |

## 五、执行顺序（严格串行，FDR 结果出来前 2.x 已冻结）

1. 写本计划（规则冻结）✅
2. quant_data.py 代码改造（#1-#10）
3. **运行 FDR 裁决** → 入选组清单落库冻结（诊断对照一并执行）
4. 种子台账（全历史）→ 跑 D2 新批次（含 S2M/S1aM，run_date=今日）
5. 跑前向步进（S2M/S1aM 从 FORWARD_START 起记录）
6. dashboard UI（#11）+ 浏览器逐项验证
7. test_module9.py + 全量回归（3/5/6/7/8/D3/逐股/组合）+ 更新受影响期望值
8. 收尾：问题日志 / SKILL 更新备份 / 项目记忆 / git 提交 + tag v1.2-module9

## 六、验收清单

- [x] FDR：BH 数学经合成 p 值单测验证；分层正确（6 族独立 m）— test_module9 [1] 3 组合成案例全过
- [x] 资格：四条件 + 覆盖率护栏逐一可追溯到入选/落选原因（eligible_reason 落库）— 18 存活行各有 reason
- [x] 诊断对照：知识截止前重合度已报告并写入 meta — 主清单 2 / 子样本 1 / 重合 1
- [x] 台账：种子幂等（重跑零新增）；守卫生效（min_date 前零插入）— test_module9 [5]
- [x] 引擎：封锁只拒新仓（持仓不受影响），S*M 成交开仓日零命中封锁日（硬不变量断言）— test_module9 [6]
- [x] 回测：新批次含 S2M/S1aM（8 策略 × 3 费率 × 2 段 + 双基准），旧批次原样保留（4 批次共存）
- [x] 前向：S2M/S1aM 净值自 FORWARD_START 追加；二次重放零 mismatch — test_module9 [8]
- [x] 管道：macro/overlay_days 两步纳入且单步失败不阻断（8 步）— test_module9 [10]
- [x] UI：三个 Tab 新区块验证非空无红 — AppTest 0 异常 + 浏览器实测（FDR 面板/④b/③b 逐一截图级核对）
- [x] 诚实披露：回测 full/oos 标注"机制对照（选择泄漏）"，唯一裁决 = forward — ④b/③b 均有警示文案

## 七、问题日志
（实施过程中追加，五要素：现象/如何发现/根因/解决/预防）

**#1 读路径 DDL 导致 database is locked**
- 现象：run_d2_backtests 运行中途抛 sqlite3.OperationalError: database is locked
- 如何发现：种子脚本跑 D2 批量回测时直接报错
- 根因：_load_overlay_blocked / load_fdr_view 等读路径里调用了 create_m9_tables（DDL），在回测写事务进行中执行 DDL 引发锁冲突
- 解决：读路径全部改为 sqlite_master 表存在性检查（无 DDL）；run_d2_backtests / run_forward_step 在开写事务前预载封锁日集与台账
- 预防：读路径永不建表（建表只在显式初始化入口）；test_module9 [11] 固化"读路径无 DDL + 预载顺序"回归检查

**#2 "全部高重要性"聚合族覆盖率恒为 0**
- 现象：入选护栏计算时 中国·全部高重要性 组覆盖率为 0，险些因"无封锁日"被误判
- 如何发现：FDR 运行日志中该组 coverage 为空 dict
- 根因：_m9_events_for_group 按字面 family 匹配事件，"全部高重要性"是聚合口径（应取该地区全部事件），宏观数据表里没有这个字面家族
- 解决：family == '全部高重要性' 分支改为按地区聚合取全部事件（方向仍按 direction 过滤）
- 预防：聚合口径字段（非字面量）在过滤器里显式特判；新增聚合口径必须带合成数据单测

**#3 run_macro_study 的 end_date 参数未生效**
- 现象：诊断对照子样本研究的事件数与全量一致，截止日不起作用
- 如何发现：诊断对照运行结果中事件数未减少
- 根因：end_date 形参定义了但 SQL 未拼接过滤条件
- 解决：SQL 动态拼接 AND trade_date <= ?；修复后主裁决研究窗口恢复原状（避免污染冻结基线）
- 预防：诊断/子样本类参数必须有单测断言行数差异（本次 test_module9 [3] 补上）

**#4 forward_meta.overlay_protocol 重复行**
- 现象：重跑 run_macro_fdr 后 forward_meta 出现多行 overlay_protocol
- 如何发现：test_module9 [8] 前向协议单行断言失败
- 根因：INSERT 前未清旧键（模块7 protocol 是一次性写入，overlay_protocol 会随重裁更新）
- 解决：改为 DELETE+INSERT 幂等写
- 预防：meta 类单行键一律 DELETE+INSERT；建表 UNIQUE 约束只防业务表不防 meta

**#5 BH 存活判据测试断言写错（step-up 语义）**
- 现象：test_module9 存活断言间歇失败：存活行的原始 p 值可以大于其 rank 临界值
- 如何发现：BH 合成案例 3（p=0.04/0.041/0.042）断言与实现不一致
- 根因：断言用了"p ≤ bh_critical"逐行判定，但 BH 是 step-up：存活 = 最大达标 rank 及其以上全部（前 rank 靠拉活）
- 解决：断言改为 q_value ≤ q（调整 p 值判据天然包含 step-up 拉活）
- 预防：多重检验实现的测试用"调整 p 值"做不变量，不要用逐行原始临界值

**#6 模块7 测试管道步数断言过时**
- 现象：test_module7 报 steps 数量不符（期望 6 实际 8）
- 如何发现：模块9 全量回归
- 根因：模块9 管道新增宏观重采 + 封锁日台账两步，模块7 测试硬编码 6
- 解决：断言改为 8 并注明步数随模块演进
- 预防：管道步数类断言写成 ≥ 基线 或注明"新增步骤须同步更新此数"

**#7 UI 封锁日覆盖率把 dict 当数字格式化（量化页整页崩溃）**
- 现象：选择"量化分析"导航后页面渲染中断，报 unsupported format string passed to dict.__format__，策略回测/前向跟踪 Tab 全部空白
- 如何发现：AppTest 全页渲染验证（当时浏览器会话锁定无法点击，AppTest 直接捕获异常堆栈 dashboard.py:2101）
- 根因：macro_overlay_meta.config.groups[].coverage 是 {股票代码: 覆盖率} 字典（按股计算），UI 代码 f"{g['coverage']:.1%}" 当数字格式化；且宏观日历 Tab 的代码段先于策略回测段执行，该异常杀死其后所有 Tab 渲染
- 解决：改为 f"{max(g['coverage'].values()):.1%}"（显示最严股票的覆盖率，与护栏口径一致）
- 预防：读取冻结配置 JSON 的 UI 代码必须先跑 AppTest 全页渲染（比浏览器点验更早暴露字段类型问题）；meta JSON 字段类型变更时同步检查所有 UI 消费点

## 八、执行结果
（交付后回填）

**交付时间：2026-09-06（模块9 全链路：FDR 裁决 → overlay 冻结 → 回测对照 → 前向集成 → UI → 测试）**

### 8.1 FDR 主裁决结果（run_macro_fdr, 2026-09-06 17:17, 配置已冻结）
- 检验规模：1,602 行（267 组 × 6 窗口族），分层 BH q=0.05（每窗口独立检验族）
- BH 存活：18 行（名义 p<0.05 有 146 行 — FDR 校正剔除约 88% 名义显著，验证了"未校正不得引用"的预判）
- Overlay 入选：**2 组**（四条件 + 覆盖率护栏全过）：
  | 地区 | 事件家族 | 指数 | 意外方向 | 封锁窗口N | n | CAR% | t | p | q | 覆盖率 |
  |---|---|---|---|---|---|---|---|---|---|---|
  | 中国 | 全部高重要性 | HSI | 低于预期 | 3 | 293 | -0.57 | -3.16 | 0.0018 | 0.0456 | 31.1% |
  | 美国 | 零售销售 | .INX | 全部 | 1 | 131 | -0.38 | -4.00 | 0.0001 | 0.0264 | 4.8% |
  （同组多窗口合格取最短 N：零售销售 [0,+1]/[0,+3] 均存活 → 取 N=1 最小干预）
- 诊断对照（知识截止 2026-05-01 前子样本，非门槛）：主清单 2 组 / 子样本存活 1 组 / 重合 1（中国·全部高重要性 在截止前已存活；零售销售 为截止后出现 — 部分稳健，仅作披露不改变裁决）

### 8.2 Overlay 机制对照回测（2026-09-06 批次，与模块8 扩窗批次同口径，4 批次共存）
| 对照 | 段 | 原版收益% | Overlay收益% | 差pp | 原版回撤% | Overlay回撤% | 原版笔数 | Overlay笔数 | 封锁次数 |
|---|---|---|---|---|---|---|---|---|---|
| S2M vs S2 | full | 114.99 | 110.84 | -4.15 | -32.65 | -32.65 | 533 | 518 | 65 |
| S2M vs S2 | oos | 7.68 | 11.52 | +3.84 | -9.24 | -7.12 | 37 | 36 | 6 |
| S1aM vs S1a | full | 91.98 | 92.47 | +0.49 | -12.89 | -12.89 | 64 | 63 | 1 |
| S1aM vs S1a | oos | 10.23 | 10.23 | 0.00 | -2.32 | -2.32 | 5 | 5 | 0 |
- 全部结果标注"机制对照（选择泄漏）非证据"；oos 段 S2M +3.84pp 且回撤收窄（-9.24→-7.12）仅为机制演示，转正/淘汰由前向 ≥20 笔裁决

### 8.3 台账与前向集成
- macro_overlay_days 台账：324 行（00700: 280 个封锁日 2023-01-12~2026-08-19；AAPL: 44 个 2023-01-19~2026-08-17；300750: 0 — A股日历与两入选家族事件窗无交集，不封锁不等于缺席）
- 前向：S2M/S1aM 自 FORWARD_START 2026-08-28 入 forward_equity（12 行），当前 0/20 样本积累中；裁决规则预注册于 forward_meta.overlay_protocol（淘汰=超额低于原版；转正=收益不降且回撤收窄；否则继续观察）
- 每日管道扩为 8 步：… → 宏观日历重采(近3天) → 封锁日台账追加(守卫=完整性截断日) → 前向记录 → 新鲜度

### 8.4 测试与验证
- test_module9.py：**62/62 通过**（BH 数学 3 案例 / FDR 一致性 / 配置冻结 / 台账幂等 / 引擎封锁硬不变量 / 策略变体 / 批次事实 / 前向集成 / 视图回读 / 管道 8 步 / 读路径无 DDL）
- 全量回归：模块3 零失败 / 模块5 31/31 / 模块6 64/64 / 模块7 39/39（步数断言更新）/ 模块8 28/28
- UI 双重验证：AppTest 全页渲染 0 异常（修复 #7 后）+ 浏览器实测三个 Tab（宏观日历⑦ FDR 面板 / 策略回测④b 机制对照 / 前向跟踪③b 裁决面板）内容与 DB 一致

### 8.5 交付物清单
- quant_data.py：_bh_stratum / run_macro_fdr / _m9_events_for_group / _load_overlay_blocked / evaluate_overlay / BT_STRATEGIES+S2M/S1aM / _run_strategy_bt(blocked_days) / 管道两步 / 前向 overlay 集成
- dashboard.py：Tab8⑦ FDR 面板（4 指标+入选家族表+分层说明+诊断对照+存活明细+台账 expander）/ Tab7④b 机制对照块 / Tab9③b 裁决面板 / 管道描述更新
- 新表：macro_fdr_results / macro_fdr_meta / macro_overlay_meta / macro_overlay_days（全 CREATE IF NOT EXISTS）
- test_module9.py / 本计划文档
