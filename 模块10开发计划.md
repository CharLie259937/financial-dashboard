# 模块10开发计划：股票池动态扩容与数据引导（入池即补数 + 前向资格区间）

> 优先级 P1（用户需求 2026-09-07：加入新股后信号采集/前向测试/历史回填仍是旧池，要求"增删股票之后能及时补充新股票数据来跟上测试进度"）。
> **目标状态**：入池即引导（行情回填→指标→信号回放→宽表，一键/自动）→ 前向测试按资格区间纳入新股（不破坏追加守卫）→ 离池/停用快照冻结（已入库净值永不失配）→ 管道自动补课 → UI 三处呈现。

## 一、现状基线（2026-09-07 实施前冻结）

| 项 | 现状 |
|----|------|
| 实测池状态 | 4 只：00700/AAPL/300750（数据齐全）+ **0100（2026-09-07 13:30 入池，0 条数据——孤儿股）** |
| add_to_pool | 仅写 stock_pool 一行，无任何数据引导 |
| 前向引擎 | `_load_backtest_data` 读活跃池 → 新股回填历史后，其前向窗口(≥2026-08-28)信号将进入触发集、且进入 B1 等权基准 → **已入库 NAV 必然失配（追加守卫告警，前向完整性破坏）** |
| 移除/停用 | remove_from_pool 级联删数据（分析侧正确）；但若该股参与过前向 → 重放成员变化 → **已入库 NAV 失配（同一根因的镜像缺陷，当前潜伏）** |
| 每日管道 | 8 步，无新股检测；batch_collect_daily 对新股只采 365 天（指标预热不足，且信号回放从未执行） |
| 前向已入库 | 10 策略 × 6 交易日（2026-08-28~09-04），完成交易 0 笔 |

**根因一句话**：入池动作与数据引导、前向资格三个环节从未打通——入池只写池行；数据靠人肉按按钮拼链；前向重放假设池构成永久冻结。

## 二、预注册规则（2026-09-07 看结果前冻结，一经写入不得依结果修改）

### 2.1 前向资格区间（Forward Membership Timeline）
1. **事件表 `forward_pool_events`**（追加式，永不改写）：`(code, event[join/leave], eff_date, source, run_ts)`
2. **存量迁移**（一次性）：池内每只已有股票补一条 join 事件，`eff_date = max(FORWARD_START, added_at 的日期部分)`——原始三只（added_at ≤ 2026-08-28）= FORWARD_START；0100 = 2026-09-07
3. **资格判定**：股票 c 的信号在交易日 d 计入前向 ⟺ d 落在某资格区间 `[join_eff, leave_eff)` 内。无事件的活跃池股票隐式视为 `(FORWARD_START, None)`（兼容兜底）
4. **新股语义**：入池当天起算资格；其回填出的**历史信号不追溯计入前向**（历史数据只用于指标预热与模块3/5分析样本），前向样本从 join_eff 起积累——与"不使用未来数据"及追加守卫同源
5. **再入池/再激活**：写新 join 事件，新区间起算；旧区间历史由快照冻结（见 2.3），互不污染

### 2.2 B1 基准冻结成员
1. **B1 只由"首批成员"构成**：首次 join_eff ≤ FORWARD_START 的股票（= 原始三只），后加入者**永不进入 B1**
2. 理由：excess_vs_B1 是裁决核心指标，必须全窗口同一基准才可比；B1 定义随池漂移会使前后段超额收益失去可比性
3. 首批成员若离池：其 B1 sleeve 冻结在最后值（现有 `_buy_hold_baseline` 的 ratio 保持语义天然支持），B2 指数基准不受池变化影响

### 2.3 离池/停用快照冻结
1. **时机**：remove_from_pool / set_pool_active(code, 0) 生效**前**
2. **内容**：该股前向窗口数据快照入两张冻结表——
   `forward_stock_px(code, trade_date, open, close)`（来自 daily_feature_base，≥FORWARD_START）
   `forward_stock_sig(code, trade_date, signal_type, signal_subtype)`（passive_signals 去重键，≥FORWARD_START）
3. **重放取数规则**：**已关闭区间**（有 leave 事件的区间）只用快照（防 qfq 重基准回写历史）；**开放区间**（当前成员）只用实时表；快照 INSERT OR REPLACE 幂等
4. **移除 vs 停用语义不变**：移除=快照+leave+级联删（分析侧零残留）；停用=快照+leave（数据保留但不进分析）；再激活=新 join
5. **验收硬指标**：任一离池/停用操作后重跑 run_forward_step，**已入库 NAV 失配数必须为 0**

### 2.4 入池引导链（onboard）
`onboard_pool_stock(code)` 顺序执行（逐步独立 try/except，失败报告不中断）：
① 行情全量回填（QUOTE_BACKFILL_START 起，复用现有市场采集函数）→ ② 深度校验 `_verify_source_depth`（本地起点晚于回填起点时用东财第二源探测真实上市起点：两源一致 ⇒ 上市新股·源端全量，无可补；东财更深 ⇒ 源缺口，补插更早区间+重叠段价格校验；顺带回填东财返回的股票名称；美股按设计跳过）→ ③ 市场基准指数检查（缺则采）→ ④ 指标全量（generate_indicators）→ ⑤ 信号回放（REPLAY_START 起，source='replay'，幂等键不变）→ ⑥ 宽表全窗口（days=3000）→ ⑦ overlay 封锁日全历史种子 `_onboard_seed_overlay`（单股 min_date=None，INSERT OR IGNORE；管道台账守卫只补截断日之后，新股历史封锁日必须在此步补齐，否则 S*M 回测把它当『历史上从未被封锁』）→ ⑧ join 事件兜底（无事件则补写）
- 顺序约束：深度校验在指标前（源缺口补插的行情先落库再算指标/信号）；overlay 种子在宽表后
- 全程幂等：重复执行零重复数据
- 可选收尾（`recalc_modules=True`）：模块3 三口径重算 + 回测新批次（池结构变化收尾三件套，费时分钟级，UI 单独按钮）

### 2.5 管道自动补课
run_daily_pipeline 新增**首步** `onboard_check`：活跃池中任一股 `quotes==0 或 wide==0 或 indicators==0 或 (quotes>0 且 signals==0)` → 自动执行该股引导链；管道 8 步 → **9 步**（test_module7 同步更新）

### 2.6 裁决连续性（纪律声明）
1. 新增成员只**增加**策略触发/交易样本来源（加速 ≥20 笔门槛积累）；裁决规则、门槛、策略参数、B1/B2 定义均**不变**
2. 池结构变化全部记录于事件表并在 Tab9 可见（透明性）；前向协议追加 `pool_protocol` 独立 meta 键说明上述规则
3. 新股行情若滞后（源端延迟）→ stock_cap（完整性截断日=池内行情终局日最小值）自动回退，净值追加暂停、恢复后自愈——沿用模块7截断语义，属诚实记录

### 2.7 已知边界（预注册披露，v1 不处理）
- 移除时该股仍有前向**在持仓位**：快照止于离池日，无法完成卖出的仓位按 end_dropped 丢弃、槽位冻结至窗口末——UI 移除按钮加警告"建议停用替代"，属可接受的极端边界
- 新股指标预热不足（如临近上市）：信号质量由 detect_signals 的滚动窗口天然保证（不足窗口不触发）

## 三、表结构变更（全部 CREATE IF NOT EXISTS，不动既有表数据）

```sql
CREATE TABLE IF NOT EXISTS forward_pool_events(
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    code TEXT NOT NULL, market TEXT, name TEXT,
    event TEXT NOT NULL,            -- join / leave
    eff_date TEXT NOT NULL,         -- 生效日(自然日)
    source TEXT,                    -- add_to_pool / remove_from_pool / set_pool_active / migrate / onboard
    run_ts TEXT
);
CREATE TABLE IF NOT EXISTS forward_stock_px(
    code TEXT NOT NULL, trade_date TEXT NOT NULL,
    open REAL, close REAL,
    PRIMARY KEY(code, trade_date)
);
CREATE TABLE IF NOT EXISTS forward_stock_sig(
    code TEXT NOT NULL, trade_date TEXT NOT NULL,
    signal_type TEXT NOT NULL, signal_subtype TEXT,
    PRIMARY KEY(code, trade_date, signal_type, signal_subtype)
);
```
建表位置：init_db 的 executescript（import 时建，与其他表同模式）；事件回填迁移为独立函数 `_m10_migrate_events()`（幂等：事件表空且池非空才写），init_db 调用、测试可单独调。

## 四、代码改造清单

### quant_data.py
1. `create tables`（init_db）+ `_m10_migrate_events()`
2. `add_to_pool`：入池后追加 join 事件（eff=今天）
3. `remove_from_pool` / `set_pool_active`：离池前 `_snapshot_forward_stock(code)`（快照+leave事件），移除继续级联删除
4. `_forward_membership()`：事件表 → `{code: [(join,leave|None),...]}`（含隐式兜底）
5. `_snapshot_forward_stock(code)`：px+sig 快照（≥FORWARD_START，INSERT OR REPLACE）
6. `run_forward_step` 改造：
   - 数据 = 活跃池实时 ∪ 快照注入（关闭区间用快照、开放区间用实时，按区间逐日裁决）
   - 触发过滤：`(c,d)` 需落在资格区间
   - B1：成员 = 首批成员（首 join_eff ≤ FORWARD_START）∩ 有数据
   - stock_cap 口径不变（活跃池）
7. `onboard_pool_stock(code, log, recalc_modules=False)` + `get_pool_onboard_status()`
8. `run_daily_pipeline`：首步 onboard_check（9 步）
9. `replay_passive_signals` 重构出单股回放helper `_replay_signals_for()`（引导链复用，行为不变）

### dashboard.py
1. Tab1：加入股票池按钮 → 自动跑引导链（st.status 逐步展示）；池表格增加 数据覆盖/前向资格日 列；新增"🚀 引导/补数据"按股按钮；移除警告文案更新；模块8回填链完成标签去掉硬编码"3 股"；新增"池结构变化收尾"按钮（模块3重算+回测新批次）
2. Tab9：新增"池构成与前向资格"块（事件时间线 + 当前资格成员 + B1冻结成员说明）

## 五、验收清单
- [x] 00100 入池引导后：行情/指标/信号/宽表四表非零（162/162/226/162），Tab1 池表显示就绪
- [x] 引导后 run_forward_step：**已入库 NAV 失配 = 0**（三只原始股逐日不变）；新股自 join 日起产生触发
- [x] 合成测试（临时库副本）：新股参与→移除→重跑，失配 = 0；停用/再激活同理（test_module10 59 项）
- [x] 管道 9 步，onboard_check 对孤儿股自动补课（test_module7 41 项含 9 步验证）
- [x] B1 成员 = 原始三只（新股加入后 B1 净值逐日不变，oos -18.37% 冻结口径验证）
- [x] 全量回归：module 3/5/6/7/8/9/10 + d3_tab 全过（详见七）
- [x] 浏览器验证 Tab1/Tab9 新块渲染（语义验证通过，截图功能环境不可用）

## 六、问题日志（五要素：现象/如何发现/根因/解决/预防）

### #1 港股代码不匹配（0100 vs 00100）→ 孤儿股
- **现象**：用户将港股 "0100" 加入股票池后，信号采集/前向测试/历史回填仍只有三只股票，新股票无任何数据
- **如何发现**：用户报告；查池表有 0100 行但 daily_quotes 无该码任何数据（孤儿股）
- **根因**：add_to_pool 未做港股代码归一化——池表存 "0100"，而行情采集统一用 5 位码 "00100"，两侧码不一致
- **解决**：add_to_pool 对港股 zfill(5) 归一化；已入库错误码 "0100" 以 join+leave 同日事件对留痕（空资格区间=零前向影响），正确码 00100 重新入池引导
- **预防**：入池入口统一归一化；管道首步 onboard_check 自动检测孤儿股（quotes==0 等）并触发引导链，同类问题自动自愈

### #2 前向引擎 end_dropped 丢弃未到期持仓 → NAV=1.0 伪空仓失真
- **现象**：前向守卫持续报 NAV 失配：S2/S2M 在 2026-08-31~09-04 有 12 行已入库净值 =1.0（伪空仓）
- **如何发现**：模块10改造后跑 run_forward_step，追加守卫 mismatch 清单不收敛
- **根因**：_run_strategy_bt 出场日超出数据末端时整笔按 end_dropped 丢弃——回测口径正确（历史末端不完整信号无法统计），但前向口径下这是"持仓未到期"，丢弃=伪平仓，NAV 回退 1.0
- **解决**：加 drop_incomplete 参数：回测默认 True（与模块5位级一致）；前向 False 时未到期持仓挂仓盯市（按最新收盘计市值、s_date=None 不触发平仓分支），出场日行情终局后由后续确定性重放补录（幂等键防重）。已入库 12 行失真值经用户批准一次性更正（DELETE+重插，forward_meta 键 nav_correction_20260908 留审计痕），重跑守卫失配=0
- **预防**：前向/回测口径差异显式参数化；守卫失配区分"引擎缺陷产物"（留审计痕后更正，避免守卫永久误报）与"真数据漂移"（只告警不改写）

### #3 回测期望值随批次重生成漂移（超额 7.40→7.64pp；费率脆弱 7→6 策略）
- **现象**：全量回归 d3_tab 期望失败且两次失败项不同：先 S1a OOS 超额 7.40 vs 实际 7.64，更新后又是费率脆弱名单 7 vs 6（S1aM 退出）
- **如何发现**：本次交付检查连跑两轮全量回归，失败项漂移
- **根因**：双因素叠加——① test_module5 每次运行调 run_d2_backtests 重生成当日批次（同日覆盖），吸收管道刷新后的 09-07 收盘 → B1 恒满仓端点移动而空仓策略 S1a 不动 → 超额漂 0.24pp；② test_module9 全历史种子在真实库执行，池扩容后新股 00100 的封锁日（+35 行）首次入库 → S1aM/S2M 重生成值变化 → S1aM oos 费后 +2.55% 转正退出脆弱名单
- **解决**：期望值更新至"最新行情+扩展后台账"重生成批次（7.64pp / 6 策略），文件头注明漂移机制；module9 "全历史种子幂等" 检查在池变更后首跑失败一次属预期（种子吸收新成员封锁日），重跑即幂等（62/62 稳定验证）
- **预防**：d3 期望值固化时机必须在完整套件跑过一轮（行情/台账收敛）之后；池结构变化后先跑 module9 种子再固化 d3 期望

### #4 Streamlit 旧进程挂旧引擎代码
- **现象**：quant_data.py 22:06 更新后，13:26 启动的看板进程仍运行旧引擎（前向盯市修复未生效风险）
- **如何发现**：交付检查时对比进程启动时间（13:26:27）与 quant_data.py 修改时间（22:06:53）
- **根因**：Streamlit 每次浏览器交互重读 dashboard.py，但 import 的 quant_data 模块只在进程启动时加载一次，服务器不重启则永远用旧模块
- **解决**：Stop-Process 后用模块命令重启（新 PID 46148），浏览器验证最新功能渲染通过
- **预防**：部署流程固化"复制代码到 Desktop 后必须重启看板"（写入 SKILL 工程约定）

### #5 新股入池缺历史宏观封锁日（overlay 台账缺口）
- **现象**：02513 智谱 09-08 引导入池后，macro_overlay_days 该股 0 行——S*M（宏观overlay）回测将其视为『历史上从未被封锁』，overlay 对其的效果被高估
- **如何发现**：用户问『为什么新进的股票回填数据之后还是只有一年不到的数据』，排查数据对齐时发现引导链产物缺 overlay 行（模块9台账追加守卫=完整性截断日之后，新股历史区间永远补不上）
- **根因**：v1 引导链 6 步无 overlay 种子步；refresh_overlay_days 的 min_date 守卫（只补截断日之后）使新股历史封锁日永久缺失
- **解决**：引导链新增第⑦步 `_onboard_seed_overlay`（refresh_overlay_days 单股 min_date=None 全历史种子，INSERT OR IGNORE 幂等）；02513 一次性补种 +35 行；SNDK 09-09 入池经新链自动获得 +19 行，验证闭环
- **预防**：入池引导链必须含全历史种子步（写入 SKILL 工程约定）；池变更后跑 test_module9 台账幂等检查，核对新成员台账行非零

### #6 新股数据"看起来没对齐"（不足一年）且名称空白
- **现象**：02513/00100 引导后行情仅 163/162 行（2026-01 上市），远少于老股 8-10 年；Tab1 池表名称空白
- **如何发现**：用户报告『新进的股票回填数据之后还是只有一年不到的数据，请将数据对齐』
- **根因**：均为 2026-01 上市新股，源端本身只有上市以来数据（非回填截断）；add_to_pool 的 name 默认空且无回填路径；美股无东财名称源（深度校验按设计跳过美股）
- **解决**：新增 `_verify_source_depth` 双源深度校验（本地起点 vs 东财不复权起点，±7 日一致 ⇒『上市新股·源端全量』标注；东财更深 ⇒ 源缺口，qfq 补插+重叠段价格校验+整段换源）；东财 kline 返回名称顺带回填 `_backfill_pool_name`；Tab1 新增『数据深度』列展示标注；美股名称人工确认（SNDK=闪迪）
- **预防**：引导链第②步自动深度校验，『数据少』自动区分『上市新股·源端全量』与『源缺口』并可视化标注；深度记录 DELETE+INSERT 落 forward_meta（source_depth:<code>）可复查

## 七、执行结果（2026-09-07 交付）

**代码**：quant_data.py +477 行 / dashboard.py +122 行 / 测试 4 文件更新（test_module10 新建 59 项 + test_module7 9 步管道 + test_module8 新股豁免 + test_d3_tab 新基准）
- 三新表 forward_pool_events / forward_stock_px / forward_stock_sig（CREATE IF NOT EXISTS + 幂等迁移）
- onboard_pool_stock 六步引导链 + _pipeline_onboard_check（管道 8→9 步，首步孤儿股补课）
- 前向资格区间（触发过滤 + B1 首批成员冻结 + 关闭区间快照/开放区间实时）
- 引擎修复 drop_incomplete（回测位级不变 / 前向盯市）
- 港股代码归一化 zfill(5)
- UI：Tab1 🚀引导/补数据 + 数据覆盖·前向资格列 + 📊池结构变化收尾；Tab9 ③c 池构成与前向资格（事件时间线 + 资格成员 + B1 冻结说明）

**数据落地**：
- 池 4 只：00100（09-07 入池，行情 162 行自 2026-01-09 上市日，源端覆盖即全量）
- 事件 6 条：3 原始股 join 2026-08-28（迁移）+ 00100/0100 join 09-07 + 0100 leave 09-07（换码留痕）
- overlay 台账 324→359（00100 封锁日 +35，模块9种子按设计扩展至新成员）
- 前向一次性更正 12 行失真净值（nav_correction_20260908 审计痕），守卫失配=0
- 回测 09-07 批次重生成：S1a oos -10.73%/超额 +7.64pp，样本外最强 S1e，费率脆弱 6 策略（S1e/S1aM 费后为正）

**回归全绿**：module3 ✓ / module5 31/31 / module6 64/64 / module7 41/41 / module8 32/32 / module9 62/62 / module10 59/59 / d3_tab 32/32

**浏览器验证**：Tab1 四股显示、00100 数据覆盖 162 行、引导/收尾按钮渲染 ✓；Tab9 池构成区块（6 条事件时间线、资格成员 4、B1 冻结 3 只）✓

**2026-09-09 数据对齐补丁（问题日志 #5/#6 交付）**：
- 池结构变化：00100 用户 09-08 18:38 移除（快照 7 行 px/6 信号留存）；02513 智谱 09-08 入池；SNDK 闪迪 09-09 16:26 用户经 UI 入池 → 现池 5 只（00700/300750/AAPL/02513/SNDK）
- 引导链 v1.1（6→8 步）：+②深度校验（双源上市起点确认/源缺口补插/名称回填）+⑦overlay 全历史种子；02513 历史缺口一次性补种 +35 行
- SNDK 经新链自动补课：行情 387 行（2025-02-24 闪迪分拆上市起）/回放信号 562 条/overlay 封锁日 19 行/名称「闪迪」人工确认（美股无东财名称源）
- 深度校验落地：00700/300750=回填对齐·全量；02513=上市新股·源端全量（双源一致 2026-01-08）；AAPL/SNDK=未校验(美股)；Tab1『数据深度』列展示
- 09-09 批次（5 股池）重生成：oos S2 最强 +93.15%/超额 90.08pp；S1a 大跌反弹 +40.26%（SNDK 触发竞争稀释了 4 股批次中 02513 极端波动主导的 +219%——槽位竞争效应，新增样本非简单叠加）；费率脆弱名单收窄至仅 S1d
- 回归全绿：module3 ✓ / module5 31/31 / module6 64/64 / module7 45/45 / module8 37/37 / module9 62/62 / module10 67/67（新增 8 项深度校验/种子检查）/ d3_tab 32/32（6 项期望重钉）
- Streamlit 服务重启加载新引擎（新进程 16:53 起，PID 61212）
