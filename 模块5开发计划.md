# 模块 5 开发计划：信号策略回测与基准对比

> 承接关系：模块 3（信号有效性统计）+ 模块 4（事件研究法）已完成 → 模块 5 是第一阶段的收尾模块
> 方法论依据：`financial-quant-dashboard-dev` skill（五步闭环 / 增量模块化交付 / 无未来数据纪律 / 踩坑经验库）
> 预计工期：3-4 天（与模块 3、4 相当）

---

## 一、模块定位与目标

### 1.1 为什么是"策略回测"而不是直接建模
- 原始 skill 文档第一阶段验收标准第 4 条至今未落地：**建立「买入持有」「大盘指数」两条基准收益线，作为后续所有模型的对比参照**——这是进入建模阶段的硬性前提
- 模块 3/4 回答的是"信号统计上是否有效"；模块 5 回答的是"**按信号实际交易，扣掉执行滞后后还能不能赚钱**"——统计有效 ≠ 策略可赚钱（隔夜跳空、信号密度、资金占用都是损耗）
- 本模块仍坚持纯统计规则、不引入机器学习，为第二阶段建模留下干净的对照基线

### 1.2 模块目标
1. 建立统一回测引擎：信号 → 持仓 → 净值曲线 → 绩效指标，全程无未来数据
2. 落地两条基准收益线：买入持有等权组合、三市场大盘指数等权合成
3. 将有效信号池中的信号策略化回测，输出与双基准的对比结论
4. 产出《策略回测对比总表》+ 分段稳健性检验，明确哪些信号"值得进入建模特征池"

---

## 二、可行性检查结论（2026-08-29 实测，Desktop 库）

按 skill 五步闭环的"可行性检查"步骤，已提前完成数据摸底：

### 2.1 数据基础（满足回测前提）
| 项 | 实况 |
|----|------|
| 宽表 daily_feature_base | 5 只股票 × 240-256 天（2025-08-21 ~ 2026-08-27），close 无空值 |
| 信号日级哑变量 | 已在宽表中（sig_macd_cross / sig_rsi_oversold / ... ），回测直接用，**禁止用 passive_signals 原始表**（该表日均 5.5 条/日，含日内多次触发，必须以宽表日级去重口径回测） |
| 基准指数 | benchmark_index 已入库（模块 4）：沪深300 5981 条 / HSI 3204 条 / 标普500 5702 条 |
| 股票池 | 300750(A) / 600000(A) / 00700(港) / 09889(港) / AAPL(美)，跨三市场 |

### 2.2 有效信号池实况（决定策略选型）
严格达标（胜率>55% & 盈亏比>1.2 & n≥30）仅 **4 组**，且全部是**超跌反弹类**：

| 信号 | 优势周期 | n | 胜率 | 超额胜率 | 盈亏比 | 超额收益 |
|------|---------|---|------|---------|--------|---------|
| price_limit\|大跌 | 10日 | 35 | 60.0% | +11.1pp | 2.03 | +2.37pp |
| price_limit\|大跌 | 5日 | 35 | 57.1% | +7.1pp | 1.56 | +1.17pp |
| boll_break\|跌破下轨 | 5日 | 88 | 56.1% | +6.1pp | 1.53 | +0.65pp |
| kdj_cross\|金叉 | 3日 | 369 | 55.0% | +4.3pp | 1.30 | +0.36pp |

主动事件面：earnings（n=87）胜率 66-83%、盈亏比 1.5-3.9，效果最强；dividend 效果弱且方向不稳定。

**选型结论**：样本池偏薄是现实约束，策略设计采取"达标信号全测 + 组合信号增样 + 事件策略单列"三层结构，并在结论中如实标注小样本风险（skill 避坑指南第 3 条）。

> **2026-08-31 更新**：上表为重复膨胀期的虚高快照——信号表幂等重建（清除 78% 重复）并落地 bearish 反向口径与 RSI24 后，池构成已变：大跌真实 n=10、跌破下轨 n=25（退出"有效"），现行唯一"有效"族为 MACD/KDJ 死叉的看空警示（反向口径，引擎不做空故不可回测）。按 D2.0 冻结协议，**S1a~S1d 维持 2026-08-29 选型不据此重选**（依新统计重选=用同一数据先看答案再选题），另增 S1e 观察策略。

---

## 三、总体设计

### 3.1 回测对象：2 组策略 + 1 观察策略 + 2 条基准

| 编号 | 类型 | 规则 | 依据 |
|------|------|------|------|
| S1 | 单信号策略 | 达标的 4 组信号各自独立回测：触发→持有其优势周期→平仓 | 模块 3 结论的直接验证 |
| S2 | 组合信号策略 | 超跌反弹组合：跌破下轨 OR 大跌 OR RSI超卖 任一触发，持有 5 日 | OR 条件合并增样（88+35+998 触发日），捕捉同类均值回归效应 |
| ~~S3~~ | ~~事件驱动策略~~ | **2026-09-02 已删除**（详见下方注） | ~~模块 3（胜率 66.7%）+ 模块 4（CAR +3.66%）双重验证~~ |
| S1e | 观察策略 | RSI24<30 触发，持有 10 日（2026-08-31 增补，观察身份） | 模块 3 观察池（10日超额 +14.9pp）；D2.0 协议的样本外验证载体 |
| B1 | 基准线 | 买入持有：5 只股票等权，回测起点建仓后不动 | 验收标准第 4 条 |
| B2 | 基准线 | 大盘指数：沪深300/HSI/标普500 等权日收益合成净值 | 验收标准第 4 条 |

> **S3 删除注（2026-09-02）**：D2 落地时 S3 oos 段 0 触发，核查发现**不是财报真空期而是事件数据源不合格**——(a) 港股"财报事件"日期实为会计年度末（12-31 报告期），非实际披露日；(b) akshare 港股财报接口 09889 映射错误（返回"东莞农商银行"）；(c) AAPL 仅一条 2026-10-29 未来日期，A股仅年报披露日（缺一季报/中报）；(d) 00700 同一事件重复入库 3 条（earnings 采集未做幂等）。因此 full 段 4 笔交易中 2 笔日期本身错误，双段结果均不可信。补采不可行（港股披露日无可靠数据源）。**处置：删除 S3**（触发源不合格的工程处置，非依回测表现重选，不违反 D2.0 冻结）。若未来重启事件驱动策略，须先重建 active_events 财报采集（A股走 akshare 预约披露表、美股走 yfinance earnings_dates、港股另寻披露日源）并修复幂等，作为独立数据工程任务。

### 3.2 回测引擎核心规则（无未来数据红线）
1. **信号确认**：T 日收盘后判定信号（实施修订 2026-08-29：信号源从 passive_signals 读取并 **DISTINCT 日级去重、保留方向**——宽表 sig_* 哑变量合并了多空方向，不适用于方向性回测；信号表已于 2026-08-31 幂等重建，零重复）
2. **成交时点**：T+1 日**开盘价**成交（买入与卖出均是）——严格晚于信号确认，杜绝 look-ahead；同时与模块 3 的"信号日收盘买入"统计口径形成差值，该差值即"隔夜跳空成本"，回测必须能暴露它
3. **收益计算**：持仓期收益 = 退出日开盘价 / 买入日开盘价 − 1（持有 N 交易日在 T+1 与 T+1+N 之间）
4. **仓位管理**：组合策略每信号事件等权分配资金，最大同时持仓数上限默认 3（参数可调），超限信号放弃；空仓资金收益记 0
5. **费用模型**：默认费率 0；提供 0.1% / 0.3% 两档敏感性开关（单边计）
6. **不做空**：bearish 信号本模块不用于开空仓，仅在结论中标注（第二阶段再扩展）

### 3.3 跨市场日历对齐（易错重点）
三市场交易日历不同（美股节假日 ≠ A股节假日）：
- 净值曲线建立在**三市场交易日并集日历**上
- 个股收益仅在其自身交易日更新；某市场休市时该股净值沿用前值（收益记 0）
- B2 指数合成同理：各指数仅在自身交易日贡献收益
- 验收时抽查：节假日（如美股感恩节）当日 A 股个股应有收益、AAPL 收益为 0

### 3.4 指标体系（每个策略与基准统一计算）
| 类别 | 指标 |
|------|------|
| 收益 | 总收益、年化收益、超额收益（vs B1 / vs B2） |
| 风险 | 年化波动率、最大回撤（含起止日期）、卡玛比率 |
| 效率 | 夏普比率（rf=0）、日胜率 |
| 交易 | 交易笔数、平均持仓天数、信号触发密度（次/月）、平均资金占用率 |
| 稳健 | 前半段/后半段分段年化收益（2025-08~2026-02 / 2026-02~2026-08，防止单段行情结论；此为 regime 稳健性口径——选择偏差控制另见 D2.0 协议的 2026-05-01 知识截止日切分，两者并存、用途分开标注） |

### 3.5 数据库新表设计（沿用模块 4 的"结果可回读"模式，2026-08-31 增 segment/fee 列）
```sql
backtest_runs     -- 每次回测运行: run_id, strategy_id, segment(full/oos), fee, params_json,
                  -- 核心指标列(总收益/年化/回撤/夏普/...), start_date, end_date,
                  -- run_date(运行日,forward test 追加式), generated_at
backtest_equity   -- 日净值: run_id, trade_date, strategy_value, bh_value, index_value
                  -- (oos run 的净值序列独立, 从 1 重起; bh/index 对照列按同窗口重算)
backtest_trades   -- 交易明细: run_id, code, market, signal_desc,
                  -- entry_date, entry_price, exit_date, exit_price, return_pct, holding_days
backtest_meta     -- 完整结果 JSON + 协议参数(知识截止日/参数冻结日/诚实性声明),
                  -- 看板重启后直接回读(模块 4 已验证该模式)
```

### 3.6 看板 Tab 8「🧪 策略回测」设计
| 区块 | 内容 |
|------|------|
| ① 参数区 | 策略多选、持有期覆盖、费率档位、最大持仓数、回测区间 |
| ② 净值对比图 | 策略 vs 买入持有 vs 大盘指数，归一化为 1 起点，Plotly 折线 |
| ③ 回撤曲线 | 各策略水下曲线（drawdown） |
| ④ 指标对比表 | 3.4 全指标，策略与基准同行对比，超额列高亮 |
| ⑤ 交易明细表 | 可按策略筛选，含每笔收益与持仓天数 |
| ⑥ 分段稳健性 | 前后半段年化对比小表 |
| ⑦ 自动结论 | "策略 X 跑赢/跑输双基准 + 跳空成本 + 样本量警示" |
| ⑧ 诊断信息 | 信号密度、资金占用率、因上限放弃的信号数 |

---

## 四、实施步骤（五步闭环 × 分日计划）

### D1：引擎实现 + 单元验证（✅ 2026-08-29 完成）
1. **探测确认（轻量）**：验证 benchmark_index 与各股行情的交易日重叠率；抽查三市场日历差异日清单——无新增外部数据源，无需新 probe
2. **quant_data.py 新增**：
   - `_load_backtest_data()`：读行情 + 被动信号日级触发集（DISTINCT 去重）+ 指数，构建并集日历
   - `_run_strategy_bt()`：槽位制事件回测核心（T+1 开盘成交，含 K 槽位与冲突处理）
   - `run_backtest(strategy_id)` / `run_all_backtests()`：净值计算 + 全指标批量执行
3. **单元验证**：`_unit_test_backtest()` 4 场景全通过（净值逐点误差<1e-12 / 含费手算一致 / K=1 冲突拒绝 / 末端持仓放弃）；2026-08-31 信号表重建后 `test_d1.py` 复跑无异常（6 策略 + 双基准，跳空成本逐笔输出）

### D2：批量回测 + 入库 + 样本内外分栏协议

#### D2.0 知识截止日与样本内外分栏协议（2026-08-31 增补，先于一切批量回测落地）

**问题**：现行全部选型——BOLL 2.0σ、池标准 55%/1.2/30、S1a~S1d 策略集、S2 组合构成——均在 2025-07~2026-08 全窗口数据上确定。若回测直接覆盖全窗口，属纯样本内回测：只能复述已知结论，收益被系统性高估，不能作为预期业绩证据。

**协议四条**：

1. **知识截止日（参数冻结日）= 2026-05-01**（266 个信号交易日的 70% 分位）
   - 样本内段：2025-07-29 ~ 2026-05-01（186 天）——全部阈值选参、池标准校准、随机基准计算只承认这段
   - 样本外段：2026-05-04 ~ 2026-08-26（80 天）——回测的"测试集"，此前从未参与任何决策
   - **诚实性声明**：参数实际在全窗口选出，样本外段属回溯性伪样本外——能检验 regime 稳健性，不能证明无选择偏差；严格意义的样本外只有 2026-09 起的 **forward test**（看板每日自动累积）
   - 自冻结日（2026-08-31）起：**禁止依据回测结果回头调整参数/池标准/策略定义**；发现问题一律记观察名单，由 forward 样本裁决
2. **回测结果强制双栏入库**（runs 表 segment 列）
   - `full`（2025-08-21 ~ 2026-08-27）：**工程验证栏**——验证 T+1 成交、K 槽位、费率、跳空成本等实现机制，收益仅作机制对照，不作业绩证据
   - `oos`（2026-05-04 起，净值从 1 重起）：**证据参考栏**——对照同窗口重算的 B1/B2 基准，作为第二阶段建模特征池的取舍依据
   - 样本外栏兼任 RSI24 的时间切分验证：58 个样本 41/17 分布在截止日两侧，2026-01 聚集段留样本内、2026-05 聚集段落样本外——转正与否由 oos 栏 + forward 表现裁决
3. **两种分段口径并存、不混淆**：3.4 的前/后半段（日历中点 2026-02）看行情段稳健性（引擎 seg1/seg2）；本协议的 2026-05-01 切分管选择偏差控制。都输出，用途分开标注
4. **forward test 接口预留**：runs 表带 run_date，后续追加新运行不动历史行；**禁止 UPDATE 已入库的 full/oos 结果**

**与策略选型的关系（冻结处理）**：见 2.2 节 2026-08-31 更新注——S1a~S1d 维持冻结选型不重选；看空警示族（现行唯一"有效"）引擎不做空（3.2 第 6 条），其结论价值在第二阶段作为规避/减仓特征，由 D3 结论层如实标注。

#### D2.1 批量回测任务清单
1. 全量运行回测对象（S1a~S1d + S1e + S2 + B1/B2 基准；S3 已于 2026-09-02 删除，见 3.1 删除注）× segment（full/oos）写入 4 张新表；oos 栏基准同窗口重算（B1 在 oos 起点等权再建仓、B2 从 oos 起点合成净值）
2. 新增观察策略 **S1e：RSI24 超卖持有 10 日**（match: rsi24_oversold|超卖）——以回测形式执行其"观察(验证中)"义务；转正条件：oos 超额为正，且 forward 累积 20+ 触发后方向不变
3. 费率敏感性：0 / 0.1% / 0.3% × 双栏（B1/B2 基准不计费）
4. **test_module5.py 独立测试脚本**：全链路（回测→入库→回读），打印每策略双栏核心指标与抽查交易
5. 重点核对：跳空成本（逐策略量化"回测收益 − 模块 3 统计收益"）、节假日对齐差异日抽查、oos 栏净值起点确为 1、full/oos 两栏触发数与信号表样本对账

#### D2.2 已知数据边界（结论层必须如实标注）
| 事实 | 影响 |
|------|------|
| ~~S3 财报事件 87 个中仅 6 个落在可回测行情区间~~ → 2026-09-02 复核查明：港股事件日期为报告期非披露日、09889 映射错误、AAPL 缺失，数据源不合格 | **S3 已删除**（见 3.1 删除注）；active_events 财报数据质量进入观察名单，模块 3 earnings 统计（n=87）同样受此影响，引用时须标注 |
| S2 组合 271 触发中 148 笔被 K=3 满仓拒绝（55%） | D3 诊断区必须呈现拒绝率；若 oos 栏同样高，容量约束是策略收益第一决定因素 |
| 大跌/跌破下轨真实样本 10/25（去重后），KDJ金叉 3日超额已转负（−1.7pp） | S1a~S1d 统计后盾薄弱，双栏结论以机制验证为主，不作特征池准入依据 |
| rsi_oversold(6) 10日超额仅 +0.7pp（"候选"级） | S2 组合主力信号统计上已弱化，组合结论须标注 |

#### D2.3 依赖现状（2026-08-31 复核：D2 可开工）
| 依赖项 | 状态 |
|---|---|
| D1 引擎 | ✅ 单元测试 4/4；冒烟测试 6 策略 + 双基准全通过（信号表重建后复跑无异常） |
| 信号数据 | ✅ passive_signals 1049 条零重复（幂等重建 2026-08-31），含 RSI24 双向 132 条 |
| 行情/基准 | ✅ daily_feature_base 264 并集交易日（2025-08-21~2026-08-27）；benchmark_index 三市场齐备 |
| 跳空成本口径 | ✅ 引擎逐笔自算统计口径收益（信号日收盘→退出日收盘），与模块 3 反向口径改动解耦 |
| 4 张回测表 | ⬜ 未建（D2 首项产出） |

### D3：看板 + 浏览器逐项验证 + 收尾
1. dashboard.py 新增 Tab 8（注意：**新标签页禁用 st.stop()，改用 if/else 条件渲染**——规避 skill 踩坑库第 1 条）
2. 浏览器自动化逐项验证：标题/净值图/回撤图/指标表/明细表/结论/无红色异常（9 项清单，与模块 4 同标准）
3. 双目录同步（工作目录编辑 → 拷贝 Desktop → 热重载生效）
4. 更新项目记忆（新表清单 + 回测口径约定）

---

## 五、风险与规避（对照 skill 踩坑经验库）

| 风险 | 规避措施 |
|------|---------|
| look-ahead（信号与成交同日） | 引擎统一"T 收盘确认 → T+1 开盘成交"，单元测试含边界日 |
| 信号样本虚增（历史重复行 / 同日多信号） | 引擎从 passive_signals 读取并 DISTINCT 日级去重保留方向；入库幂等（2026-08-31 重建后 1049 条零重复） |
| **回测全窗口 = 样本内复述，业绩被高估** | **D2.0 协议**：知识截止日冻结 + full/oos 双栏 + forward test 唯一严格样本外；禁止依回测结果回头调参 |
| 新标签页空白（st.stop() 阻断） | Tab 8 全部条件渲染，不用 st.stop() |
| 小样本过拟合（n=10~25 级别，去重后真实样本） | 结论强制标注样本量；分段稳健性不通过者降级为"观察池" |
| 跨市场日历错位 | 并集日历 + 休市收益记 0，D1 抽查差异日 |
| 双 Python 环境混淆 | 全程使用 Python314 解释器运行 |
| 结果重启丢失 | backtest_meta 存完整 JSON（模块 4 模式），含协议参数 |

---

## 六、验收清单（模块完成定义）

- [x] 引擎单元测试：人工样本净值手算对照误差为 0
- [x] B1/B2 两条基准净值曲线入库并在看板可展示（补齐第一阶段验收第 4 条）
- [x] 8 个回测对象（S1×4 + S1e + S2 + 双基准；原 S3 已于 2026-09-02 删除，见 3.1 删除注）全部产出指标 + 交易明细
- [x] 每策略输出 vs 双基准的超额收益、回撤、夏普对比
- [x] 跳空成本量化呈现（模块 3 统计收益 vs 回测收益差值）
- [x] 分段稳健性 + 费率敏感性结果入库
- [x] **知识截止日 2026-05-01 / 冻结日 2026-08-31 记录于 backtest_meta；full/oos 双栏结果入库**
- [x] **oos 栏对照同窗口重算基准；样本外结论标注"回溯性伪样本外"；forward 接口（run_date 追加式）可用**
- [x] 看板 Tab 8 浏览器逐项验证通过（无红色异常）
- [x] 《策略回测对比总表》含明确结论：哪些信号进入第二阶段建模特征池、哪些降级观察（看空警示族单独成节，标注"不做空、留作规避特征"）
- [x] 双目录文件同步一致，测试脚本归档

> 验收记录（2026-09-01 D3 完成时全项勾选；2026-09-02 S3 删除后重跑 D2/D1/D3 测试全绿：test_module5 27/27、test_d1 回归通过、test_d3_tab 31/31，库内 40 runs / 2776 fee0 净值行 / 1119 交易行，S3 零残留）

**完成即宣告第一阶段全部收尾**（数据层/统计层/结论层/基准层四条验收标准齐备），可启动第二阶段建模。
