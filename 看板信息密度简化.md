# 看板信息密度简化（2026-09-19/20 三批次交付）

> 起因：用户指出看板内容冗余、后续还要持续添加（D4 风险分解、模块13 等）。
> 根因诊断：12 个模块累加式交付，每个模块在自己的层加视图，从未做过信息架构收纳——
> 9 个 Tab 共约 100 个平铺区块标记、仅 12 个折叠框，几乎所有内容直接渲染。
> 原则：**只折叠不删除**（结论可回溯红线），档案默认收起，零数据层改动。

## 一、信息分层规则（新增内容的归位标准）

| 层 | 判断标准 | 展示方式 |
|----|---------|---------|
| **裁决层** | 每天要看：前向裁决、回测总表、核心信号池、封锁台账 | 直接平铺 |
| **操作层** | 用到才看：采集、池管理、管道手动运行 | 平铺/折叠均可 |
| **档案层** | 结论已冻结的研究视图、单次运行深挖、诊断明细 | **📁 expander 默认收起 + 密度开关联动** |

- 后续新增区块先回答"属于哪一层"；档案层一律 `📁` 前缀 + `expanded=ARCHIVE_EXPANDED` 参数。
- sidebar「展示密度：精简（默认）/完整」开关仅量化分析页显示，14 个档案框统一联动。

## 二、三批次交付明细

### 批次1（88c08b1）T6 信号效果 + T8 宏观日历
- T8 `④⑤⑥` 宏观事件研究（模块6，结论 2026-09-06 冻结）→ 单一档案 expander
- T8 `⑦` FDR 块从 ④ 的 `else:` 内部**提级到顶层**——修复潜伏嵌套：此前宏观研究结果为空则 ⑦（含封锁台账）整块不渲染
- T8 **封锁日台账提级**为 ⑦ 内直接展示（原埋在三层嵌套 expander 里，用户找不到"当前封锁的股票"）；展示顺序：入选家族 → 封锁日台账（直接表）→ BH 存活组明细（expander）
- T6 O1 分市场分层（09-18 交付）从 `if sig_rows:` 内部提级为独立区块保持可见
- T6 三组收纳：被动明细+胜率/超额图表 / 按股票分组+差异分析 / 主动事件+发布时点
- 保留平铺：核心有效信号池、RSI24 裁决、分市场分层、随机基准

### 批次2（3e16f95）T7 策略回测 + T9 前向跟踪
- T7 五个单次运行深挖视图收纳：⑤绩效明细 / ⑥费率敏感性 / ⑦交易明细 / ⑧分段稳健性 / ⑨容量诊断
- 保留平铺：⚙️运行管理 / ①参数 / ②净值 / ③回撤 / ④总表 / ④b④c机制对照 / ⑩自动结论
- T9 ① 每日管道收纳 expander + **外置一行状态摘要**（最近运行时间 + 成功步数，每日瞥一眼即可）
- T9 ⑥ 前向交易明细收纳；保留：②新鲜度 / ③裁决家族 / ④净值 / ⑤快照 / ⑦衰减 / ⑧瘦身 / ⑨-⑬

### 批次3（4874647）T1/T4 去重 + 密度开关
- sidebar 密度开关（精简默认/完整），14 个 📁 档案框联动 `expanded=ARCHIVE_EXPANDED`
- T1 宽表预览与信号触发明细收纳
- T4 数据分布·覆盖·日线图收纳一档；估值两区块收纳一档（已知缺口#5：PE/PB 回填段为零，观察档案）
- T4 保留平铺：4 张总览指标卡（页面身份）

## 三、验证与回归

- `test_density_simplify.py`（新增）：40 检查 / 5 阶段（全新会话 / 注入 m3 / 批次2 / 批次3 精简 / 切换完整模式重跑）全部 0 异常
- `test_s2_apptest.py` PASS（顺手修正陈旧 progress 期望 7→10 张卡）
- 全量回归：m3 ✓ / m5 41/41 / m6 64 / m7 50 / m8 37（首跑 1 败未复现，m5→m8 连跑与单独跑均 37/0，判定瞬时源抖动）/ m9 62 / m10 67 / m11 39 / m12 79 / d3 32（09-20 重钉：S1a 28.06pp、S1e +0.64pp，B1 端点随新交易日上移挤压超额，已知模式）

## 四、工程备注

- 补丁脚本 `_patch_simplify_b1/b2/b3.py` 随库留档（锚点式字符串手术，重跑前需 git checkout 还原 dashboard.py）
- 密度开关位于导航 radio 之后，AppTest 的 `sidebar.radio[0]` 仍指向导航，既有测试不受影响
- Streamlit expander 内容**始终执行**（只是渲染收起），收纳不改变任何计算/入库行为
