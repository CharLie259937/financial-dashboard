# 模块12开发计划：事件层事前整合（S3 → S1aE/S2E 预注册）

> 起因：外部评价最有力批评之一"事件层未进信号链路，S1a/S2 是无事件感知的均值回归"（优化清单 2.2，采纳）；S1 池外对照研究（2026-09-15）进一步证实现有 S1a/S2 因子池外不复现，改进优先级让位于事件层与前向样本累积。
> 结论：以 meta-labeling 两层框架（López de Prado 2018, AFML 第3章）预注册变体 **S1aE / S2E**——第一层=现有技术触发（不动），第二层=事件门控（错杀三条件）决定放行与否。**v1 冻结为二值门控（只做减法）**，仓位放大系数留二期单独预注册。裁决权归前向测试。
> 定位：**本文档即预注册文档**——变体定义与裁决规则在看任何 SxE 前向样本之前冻结（2026-09-15）。实现分 P1（采集层）/ P2（引擎接入）/ D3（看板 UI）三期，P1 为前向计数启动的硬前置。

## 一、路径修正史（为什么是 meta-labeling，而不是重建 S3）

| 时间 | 事件 | 教训 |
|------|------|------|
| 2026-09-02 | 删除策略 S3（事件·财报）：akshare"财报日期"实为会计年度末（非披露日）、Yahoo 仅提供下次财报日（未来日期），full 段 4 笔交易中 2 笔日期本身错误 | 事件数据源不合格时，事件策略不可运行——**先修数据层再谈策略** |
| 2026-09-03 | active_events 财报采集停用，仅分红留存（有效样本 10 个：7 分红 + 3 财报） | 个股事件 ≠ 仅有财报；分红源经审计合格 |
| 2026-09-15 | S1 池外对照：对照组 8 股 S1a 单笔 +1.41% vs 池内 +10.46%，池外不复现 | 现有技术因子无池外放大来源；**增量必须来自新信息层（事件），不是参数微调** |

外部评价暗示的超图/LLM 路径不可行（n=162 样本，见优化清单第4节质疑）。meta-labeling 是唯一与现有 forward 框架完全兼容的事前整合路径，且与模块9 overlay"只做减法"机制同构——引擎零改动即可复用。

## 二、S1aE / S2E 变体定义（预注册冻结，2026-09-15）

### 2.1 两层决策框架

```
L1 触发层（原版不动）        L2 门控层（新增，逐条检查）
S1a: price_limit/大跌  ──→  C1 事件空白 ─ C2 技术超卖 ─ C3 财报远期
S2:  跌破下轨/大跌/超卖 ──→  （同款三条件）
                              │
              全过 → 放行（正常 T+1 开盘入场，槽位/持有期/费率与原版一致）
              任一不过 → 拒绝开仓（该触发丢弃，不占槽位）
```

- **唯一差异 = 开仓前的事件门控**，持有期（S1aE=10日 / S2E=5日）、槽位、费率、出场规则与原版完全一致
- 触发/持有/费率与原版一致 + 只做减法 ⇒ 前向重放确定性，与 S2M/S1aM（模块9 overlay）同一机制模式
- 回测段（full/oos）结果一律标注**机制对照（选择泄漏）**：三条件由含知识截止后数据的研究窗口归纳；唯一裁决 = 前向测试

### 2.2 错杀三条件（可操作化口径，先验冻结）

| # | 条件 | 口径（冻结） | 依据 |
|---|------|--------------|------|
| C1 | 事件空白 | 信号触发日 T（收盘确认日）前 5 个交易日（含 T）内，该股在 active_events 无生效事件（当前有效类型：分红） | 事件后价格未消化完毕的下跌是信息驱动，非错杀；5 日覆盖分红除权波动窗 |
| C2 | 技术超卖 | 触发日 RSI14 < 30 | 错杀的技术确认：跌至超卖极值而非趋势中继 |
| C3 | 财报远期 | 下次财报披露日 − 触发日 ≥ 10 个交易日 | 财报窗前的下跌是定价不确定性，非错杀；10 交易日覆盖财报提前/延后漂移 ±1 周 |

**边界诚实披露**：
- C2 对 **S2E 近似恒真**（S2 触发集已含 rsi_oversold/超卖）——S2E 的实际门控由 C1/C3 承担；对 S1aE 有独立过滤价值（大跌≠必然超卖）。此为设计事实，非缺陷
- C3 依赖财报日历，**当前无合格数据源**（见 S3 删除根因）→ C3 的数据前提即模块12 P1
- 量价确认（缩量大跌胜率 93.3% n=180 vs 放量 55.2% n=348）为**候选第 4 条**，一期不启用：该分层来自池内历史统计，直接启用有选择泄漏风险；先让三条件版本建立基线，第 4 条走二期预注册 + 前向裁决，不回头改已有条件

### 2.3 仓位放大系数：二期，不进 v1

优化清单原文含"事件标签决定放行与否**与仓位放大系数**"。v1 明确不做放大，理由：
1. 现有引擎为槽位制（每槽 1/K 资金），per-trade 加权需引擎改造，波及冻结回测可复现性
2. 三条件全过 vs 不过的分层胜率对比**尚无任何数据**——现在预注册放大倍数属于无据先验
3. 若 v1 门控前向显示正增量，二期以实测分层差异为据单独预注册（新文档，不改本文）

## 三、裁决规则（预注册冻结，不得依前向结果修改）

- **门槛**：S1aE、S2E 各自完成 ≥ 20 笔前向完成交易后裁决（FORWARD_ADJ_MIN_TRADES 同款）
- **对照**：S1aE vs S1a、S2E vs S2 前向**并行对照**（同窗口、同池、同协议），另报超额 vs B1
- 判定（对每个变体独立执行）：
  - 🔴 **淘汰建议**：超额 vs B1 ≤ 0（门控版自身不赚钱）
  - 🔴 **无增量淘汰**：超额 vs B1 > 0 但 ≤ 原版同窗超额（门控拖累或无贡献——减法变体触发更少，样本积累更慢，此判定需耐心）
  - 🟢 **转正候选**：超额 vs B1 > 0 **且 > 原版超额** 且 胜率 ≥ 原版（门控有净正贡献）
  - 🟡 其余：继续观察
- **协议存储**：独立 meta 键 `forward_meta.s3_event_protocol`（与 overlay_protocol 同模式，DELETE+INSERT 幂等），不触碰主协议与 overlay 协议
- freeze_note：裁决规则预注册于 2026-09-15（本文档第三节），不得依前向结果修改，与回测冻结协议同源

## 四、数据前提（P1 采集层）与前向计数启动闸门

**启动条件（全部满足才启动 SxE 前向计数）**：

| # | 前提 | 验收标准 |
|---|------|----------|
| 1 | 财报日历 E1/E2 双源 | 候选源：yfinance `get_earnings_dates` / Nasdaq earnings API；验收：历史披露日双源一致率 ≥ 95%，或单源抽样 20 条人工核对通过 |
| 2 | 分红事件 | 已满足（2026-09-03 审计后留存，akshare/yahoo） |
| 3 | C3 参数冻结登记 | P1 验收通过当日，将"前向计数起点日"写入 s3_event_protocol；**起点日之前的 SxE 触发不入样本**（无财报日历期间无法计算 C3，样本口径不完整） |

P1 验收前，S1aE/S2E 在看板显示"🔵 预注册·待采集层"状态，不产生任何交易记录。

## 五、交付物分期

| 期 | 交付物 | 内容 |
|----|--------|------|
| P0（本文档） | 预注册文档 + 看板挂载 | 模块12开发计划.md；quant_data.py 常量 `S3_EVENT_PREREG`；D1 悬置假设卡片新增 S1aE/S2E 两张（🔵 预注册·待采集层） |
| P1 | 财报日历采集层 + D3 UI 前半 | earnings_calendar 表（E1/E2 双源、UNIQUE(code,event_date,source)）；双日历保护矩阵视图（个股财报周 × 宏观封锁日，P0–P3 等级）；采集健康度（E1/E2 连续成功天数） |
| P2 | 变体引擎接入 | BT_STRATEGIES 注册 S1aE/S2E（`event_gate: True` 标记，match/hold 沿用原版）；forward 重放扩展门控逻辑；`evaluate_s3_event()` 裁决函数；回测段跑机制对照批次（标注选择泄漏） |
| D3 后半 | 错杀候选实时标记 | 三条件逐项打勾的当日触发判定列表（依赖 P2 门控函数） |

## 六、冻结协议合规声明

1. 原版 S1a/S2 定义、参数、前向记录**一律不动**（只新增变体）
2. 主前向协议 `forward_meta.protocol` 不改（SxE 用独立 s3_event_protocol 键）
3. 变体定义与裁决规则冻结于 2026-09-15（本文档），冻结日后禁止依回测/前向结果回头修改三条件口径、阈值或判定规则
4. 回测段 SxE 结果仅作机制对照展示，不作为转正/淘汰依据；唯一裁决 = 前向测试（与 D2.0 知识截止协议同源）

## 七、验证计划

- P0：test_s3_prereg 检查——常量存在性、卡片渲染（AppTest 全页 0 异常）、文档与本文件一致性
- P1：E1/E2 一致率报告 + 采集幂等重跑零新增 + D3 视图渲染
- P2：前向重放确定性（同数据重跑 SxE 净值零漂移）+ 门控拒绝路径单测（构造 C1/C2/C3 各不过的触发样本，验证拒绝）+ 全量回归（m3/m5/m6/m7/m8/m9/m10/m11/d3 全绿）

### P0 验证记录（2026-09-15）

- AppTest 全页渲染：**0 异常 PASS**——⑦ 信号衰减监控 / ⑧ 信号采集瘦身 / ⑨ 前向协议 / 悬置假设进度卡片 / 裁决规则回读 / S1aE+S2E 预注册卡片×2 全部 OK，progress 组件 7/7
- 全量回归：m3 ✓ / m5 31 / m6 64 / m7 45 / m8 37 / m9 62 / m10 67 / m11 39 / d3 32 —— **全绿**
- 回归附带发现并修复两组存量问题（非 P0 引入，git stash 对照验证）：
  - d3 三处期望漂移（行情端点移动 + m5 重跑重生当日批次）：S1b 超额 70.01pp 反超 S2 居首、S1a 42.22pp、S1e +9.36pp，按 09-10 同款惯例重钉，期望演进记入 test_d3_tab.py 头部
  - m11 四项断言时序脆弱（粒度上限/美股盘中幂等/种子一次性状态），修正断言语义恢复 39/39，问题日志见 模块11开发计划.md #1

## 八、P1 交付记录（2026-09-16）

### 8.1 探测结论（探测脚本 `_probe_e1_p1.py` / `_probe_e1_p1b.py`）
- **东财公告流** `np-anotice-stock`（A/H 同 API）：翻页 100 条/页；A股 E1 标题（年度报告/半年度报告/第一·三季度报告）与港股 E1 标题（业绩公布/業績公布，简繁双写法）实测全命中；E2 关键词（配售/收购/回购/解禁/澄清/增减持）实证有效（智谱 07-09 配售、07-07 澄清与研究报告案例吻合）→ 定为 A/H 的 E1+E2 主源，**全量入库按规则分类，未匹配落 unclassified**
- **SEC EDGAR** submissions：`items` 字段确认（8-K items 含 `2.02` = 财报发布）；AAPL CIK 0000320193、SNDK（2025 分拆新主体）0002023554；acceptanceDateTime 可判盘前/盘后 → 美股主源（仅采集 8-K）
- **百度 sapi report_time**：**锚定日交叉验证 5/5 命中**（300750 07-25 / 00700 08-12·03-18 / AAPL 07-30 / SNDK 08-05，按 code 字段精确匹配）；历史深度 ≥2025-08；**未来日有预约排期**（他司代码可见）→ 交叉验证源 + C3 排期来源
- **失效源**：`RPT_PUBLIC_BS_NEW` 返回"报表配置不存在"（研究期可用，现已下线）——不依赖；`RPT_HKFN_MAINFINADATA` 同样失效且本无披露日语义，弃用

### 8.2 表与工程修正
- `earnings_calendar`：dedup_key = sha1(code|date|source|class|subtype|period|title) UNIQUE——**对计划文档简化键 UNIQUE(code,event_date,source) 的工程修正**：同日同源多条公告（如配售+澄清同日）必须可共存，简化键会静默丢行；冻结面（三条件/裁决规则）不受影响
- `earnings_collect_log`：追加式运行日志（健康度=按日无 FAIL 的连续天数）
- 时区：自实现美东 DST（3月第2个周日02:00~11月第1个周日02:00 本地切换，不依赖 tzdata——Windows 无系统时区库）

### 8.3 采集事实（2026-09-16 首采）
| 股 | 源 | 入库 | 覆盖 |
|----|----|------|------|
| 00700 | eastmoney_notice | 1396 条 | 2006-11-22~2026-09-16（20 年公告流） |
| 02513 | eastmoney_notice | 55 条 | 2025-12-19~（上市全量） |
| 300750 | eastmoney_notice | 1400 条 | 2020-07-17~ |
| AAPL | sec_edgar | 103 条 8-K | 2015-07-31~ |
| SNDK | sec_edgar | 15 条 8-K | 2025-02-03~（分拆后全量） |

- **双源一致率 30/30 = 100.0%**（最近 30 个主源 E1 日期在百度 report_time 精确 code 命中；歧义日 0）→ **P1 验收闸门 ≥95% 通过**
- 锚点核对：02513 2026-03-31 年度业绩公告 ✓（模块12研究案例）、300750 2026-03-10 年报 ✓（经济日历审计锚点）
- 未来 75 天排期 0 条 = 源端事实（抽查未来日均为他司排期；A股三季报预约表未发布、港美排期临近才出）——机制已由合成排期行验证，预约发布后重跑采集自动补充（幂等）
- **C3 前向计数起点已登记：2026-09-16**（forward_meta.s3_event_protocol，DELETE+INSERT 幂等；S1aE/S2E 卡片联动显示"采集层就绪·待P2引擎接入"）

### 8.4 UI（D3 前半）
- Tab1 新增「📅 财报日历」卡：覆盖表（E1 日数/起止/下次财报/E2 分类计数）+ 一致率三指标 + 健康度 caption + 采集按钮（st.status 流式，约 3-5 分钟）
- Tab9 新增「⑩ 双日历保护矩阵」：池内股票 × 未来 14 天 P0/P2/P3（📈财报周 🔒宏观封锁 ⚠️双叠加）；财报周展示用 ±3 自然日包络（±2 交易日的保守近似，未来日期无交易日历，P2 引擎接入时精化）

### 8.5 验证
- test_module12.py **49/49**（分类配置简繁与顺序敏感 / DST 边界 / dedup 幂等与同日多公告 / 建表幂等 / 读路径无DDL / 矩阵等级与池覆盖 / C3 登记幂等+原值恢复 / live 锚点+一致率）
- AppTest 全页 **0 异常**；全量回归 m3 ✓ / m5 31 / m6 64 / m7 45 / m8 37 / m9 62 / m10 67 / m11 39 / d3 32（S1a 43.27→43.75pp 当日二次重钉：17:41 补跑管道吸收当日到货港股/指数端点，期望演进记 test_d3_tab.py 头部）
- 采集幂等：重跑零新增（dedup_key INSERT OR IGNORE，live 段测试守卫）

### 8.6 问题日志
**#1 _to_et 初版 DST 按日粒度切换，边界日差 1 小时**
- 现象：test_module12 DST 切换日断言失败（2026-03-08 05:00Z 期望 00:00 EST 实得 01:00 EDT）
- 如何发现：交付前自写测试的边界样本
- 根因：初版以切换日 00:00（UTC 口径）为界，实际切换发生在 02:00 美东本地
- 解决：改为标准算法——先按 EST(-5) 假设计算本地候选，候选落在 [3月第2个周日02:00, 11月第1个周日02:00) 内则用 EDT(-4)
- 预防：时区/DST 类自实现必须有切换日前后两侧的边界断言（05:00Z/07:00Z 双样本）；影响面评估：EDGAR 接收时间 06:00-22:00 ET，1 小时误差不改变盘前/盘中/盘后分类，但按正确性修复

**#2 RPT_PUBLIC_BS_NEW 研究期可用、交付期失效**
- 现象：探测脚本返回 success=False "报表配置不存在"
- 如何发现：P1 探测第一步（探测先行纪律拦截，未进入实现）
- 根因：东财 datacenter 报表配置下线/更名（第三方报表接口生命周期不受控）
- 解决：A 股 E1 改走东财公告流标题规则（与港股同链路，少维护一个接口）
- 预防：外部报表类接口（reportName 级）不作为唯一源；标题规则跑在公告流上更稳（公告流是面向终端用户的展示接口，生命周期长于 datacenter 报表）

**#3 test live 段"≥2 股有下次财报日"断言绑定暂时性源状态**
- 现象：首采后该断言失败（0 股），但机制正常
- 如何发现：live 段运行
- 根因：排期是否发布是源端暂时状态（A股三季报预约表未出），把源内容多少当成了机制正确性断言——与 m11 问题日志#1 同类错误
- 解决：改为结构断言 + info 打印（机制已由 [4] 合成排期行在矩阵上验证）
- 预防：live 断言只验证"机制与结构"，不验证"源端当前有没有数据"；源内容类检查一律 info 化或带 SKIP 守卫

## 九、P2 交付记录（2026-09-18，与波动域门控 P1 同批引擎实现）

### 9.1 交付物
- **BT_STRATEGIES 注册 S1aE/S2E**（event_gate: True，match/hold 与原版逐位一致，fwd_start 2026-09-16）+ 同批注册 S1bV/S2V（波动域门控，权威文档《波动域门控变体预注册.md》）
- **引擎门控注入口**：_run_strategy_bt 新增 gate_fn 参数（触发期评估，gate_blocked 计数）——与模块9 blocked_days 正交可叠加；run_backtest（懒加载）/run_d2_backtests（预载复用，写事务前——坑3 纪律）注入；run_forward_step 按 fwd_start 过滤触发（计数起点前触发不入样本）
- **门控上下文** _load_gate_context：volatility20（前移一日防自指）/rsi14/E1日历（含排期）/active_events；快照缺指标股保守拒绝
- **evaluate_s3_event()** 裁决读数（≥20 笔后按冻结规则四档判定）；同构 evaluate_vol_gate()
- **D3 后半**：Tab9「⑪ 错杀候选实时标记」（近45天 S1a/S2 触发族三条件逐项打勾）+「⑫ 门控变体裁决读数」表；D1 悬置卡片 7→9 张

### 9.2 机制对照批次（2026-09-18，76 runs = 12策略×2段×3费率+双基准）
| 变体 | 原版full笔数 | 门控拦截 | 备注（机制对照·选择泄漏·非证据） |
|------|--------------|----------|----------------------------------|
| S1aE | 127 | 116 | oos 段 0 笔——C3 对「下次财报未知」保守拒绝（排期缺位，按预注册生效） |
| S2E | 618 | 535 | 同上，oos 0 笔 |
| S1bV | 178 | 65 | full 收益 +710% vs S1b +801%——门控拒掉的触发并非全为负贡献 |
| S2V | 618 | 481 | 硬不变量：SxV 开仓触发日 100% 高波动域（250 笔全查 PASS） |

前向首批读数：S1aE 触发 4 全被门控拦截（C3 生效）、S2E/S1bV/S2V 触发 0——**S1aE/S2E 前向计数实际冻结至财报排期发布**（当前源端排期 0 条，诚实披露）；S1bV/S2V 自 2026-09-18 起积累。前向步进 56 行新净值（4变体×14日）零失配。

### 9.3 验证
- test_module12 **79/79**（新增 [8] 门控批次 30 项：注册一致性/门控单元边界(C-V 0.80、C2 RSI30、C1 窗口、C3 距离与保守拒绝)/机制对照硬不变量/fwd_start 纪律/评估器/卡片/读路径无DDL）
- 全量回归：m3 / m5 **39**（12策略动态+8）/ m6 64 / m7 **49**（+4 策略相关断言）/ m8 37（新信号首跑吸收后）/ m9 62 / m10 67 / m11 39 / d3 32（09-18 批次重钉：S1aM 58.67pp 微弱居首、S1a 43.23、S1e +5.99、费率脆弱=S1aE/S1d/S2E——门控变体笔数少费后为负属预期）
- AppTest 全页 0 异常

### 9.4 问题日志
**#4 [6] 段收尾断言绑定「生产库无登记」假设**
- 现象：P2 交付后 test_module12「原值恢复后卡片回落未登记态」失败
- 如何发现：门控批次全量回归
- 根因：该断言写于 P1 交付前（s3_event_protocol 尚未真实登记），隐含「恢复后=未登记态」；P1 验收登记后原值即为登记态
- 解决：断言改为「恢复到登记前原状态」（orig 为空→未登记态，否则→就绪态）
- 预防：涉及「恢复原值」的测试不得对生产库当前状态做隐含假设，断言目标应是「与备份一致」而非某个固定态
