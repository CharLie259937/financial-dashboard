# 模块8开发计划：样本扩展与信号回放

> 优先级 P1（前向等待期计划第一优先）。依据：回测冻结协议允许数据扩展；当前行情仅 13.5 个月（2025-02-25 起），模块 3 多数信号处于"候选/小样本"，RSI24 时间分割验证因样本不足无法完成（后 30% 段仅 3 触发）。
> **目标状态**：行情回填至 2020（预热）→ 信号回放自 2021 → 模块 3/5 扩窗重算 → RSI24 时间分割出结论。

## 一、探测结论（2026-09-04，实施前冻结）

| 探测项 | 结果 | 决策 |
|--------|------|------|
| akshare 三股全历史（stock_zh_a_daily / stock_hk_daily / stock_us_daily, qfq） | 300750 自 2018-06（IPO 起）；00700 自 2004-06；AAPL 自 1984-09；≥2020-01-01 各 1619/1642/1677 行 | 行情回填至 2020-01-01 可行，现函数仅需放宽 days 参数 |
| 宏观日历源历史深度（百度财经日历, curl_cffi 客户端） | 2023-01-05 起有数据（66 条）；2022-12-15 及更早返回 0 条 | **宏观回填目标从 2021 修正为 2023-01-01**（源深度硬限制，非代码问题） |
| 现有数据基线 | quotes 1127 行（2025-02-25 起）；passive_signals 792 行（2025-07-29 起，实采口径）；宽表 768 行（2025-08-21 起） | 回放窗口 [2021-01-01, 2025-07-29) 无信号无行情，全部由本次回填补齐 |

## 二、核心设计决策（2026-09-04 冻结）

### 2.1 窗口锚点
- `QUOTE_BACKFILL_START = '2020-01-01'`：行情回填起点。多回一年做指标预热（MACD/RSI24/BOLL 滚动计算需要历史），信号不早于 2021
- `REPLAY_START = '2021-01-01'`：回放信号记录起点（约 4.6 年回放窗 + 13.5 月实采窗）
- `MACRO_BACKFILL_TARGET = '2023-01-01'`：宏观回填起点（源深度限制）
- 边界诚实披露：实采信号自 2025-07-29 起；[2021-01-01, 2025-07-29) 为回放口径；2025-02-25~2025-07-28 有行情但当时未装扫描器，该段信号由回填补齐（source='replay'）

### 2.2 前复权基准变化（重要）
回填用最新前复权（qfq）基准重写**全序列**（INSERT OR REPLACE），历史价格会与库内旧值有微小差异（分红导致的复权基准移动）。这是正确行为——全序列口径统一；模块 3/5 重算自动使用统一口径。交界月份（2025-02/03）抽样比对连续性作为验收项。

### 2.3 回放引擎 = 现有引擎原样重放（口径零改动）
- 复用 `detect_signals`（14 条规则全部可由指标+行情确定性重算）+ 既有幂等键（code+trade_date+signal_type+signal_subtype+direction）
- `passive_signals` 增加 `source` 列（TEXT DEFAULT 'live'）：存量 792 行自动为 'live'；回放插入标 'replay'
- 回放扫描全历史 [REPLAY_START, 今日]，幂等键保证：已存在的 live 行不重不覆，仅补缺口；重复运行零新增
- 实采段（≥2025-07-29）的扫描缺口也会被回放补齐并如实标记 source='replay'（诚实披露来源，双口径统计可分离）
- 指标与宽表：`generate_indicators`/`generate_wide_table` 本就全窗口/参数化窗口，回填后调用一次 days=2500 全量重建；每日管道仍按 days=365/400 刷新近端，旧数据行持久保留

### 2.4 模块 3 双口径统计
- `compute_passive_signal_stats` 增加 source 过滤；`run_module3_analysis` 计算三口径：**合并**（replay+live，默认展示）、**replay**（仅回放段）、**live**（仅实采段）
- `signal_effect_report` 增加 `caliber` 列（'合并'/'replay'/'live'）；主动事件无回放概念，仅'合并'口径
- 有效池/评级基于**合并**口径（样本最大）；replay/live 口径供对照，防"回放口径污染实采结论"——若两口径结论背离，以 live 口径为观察方向、合并口径为统计资格

### 2.5 RSI24 时间分割验证规则（预注册，看结果前冻结）
- 对象：`rsi24_oversold`（bullish/normal）与 `rsi24_overbought`（bearish/reverse），合并口径
- 分割：信号按 trade_date 升序，前 70% 为段A（开发段），后 30% 为段B（验证段）；分割点日期写入结果
- 判定（基于 10 日持有期，与 S1e 定义一致）：
  - **转正**：段A、段B 各自触发数 ≥ 20，且两段超额胜率（vs 同口径随机基准）均 > 0，且两段平均超额收益均 > 0 → 状态 '观察(验证中)' → '有效(时间分割通过)'
  - **淘汰**：任一段超额胜率 ≤ −5pp 或 平均超额收益 ≤ −3pp → '淘汰(时间分割未过)'，仅展示不入池
  - **其余**：继续观察，等 forward test 并行证据
- 规则一经写入不得依结果修改（与回测冻结协议同源）；其余信号类型的时间分割结果仅作诊断展示，不设预注册判定
- 随机基准随扩窗自动重算（同口径对比）

### 2.6 模块 5 扩窗重跑（仅记录）
- 调用 `run_d2_backtests()` 生成新 run_date 批次：full 段起点从 2025-02 前移至 2020-01；**oos 段起点不变**（=知识截止 2026-05-01，按日期划界不受回填影响）
- 全部冻结参数原样执行（BT_STRATEGIES/K/费率档），不因结果调整任何参数；旧批次（2026-09-03）保留作对照
- 前向测试（模块 7）不受影响：FORWARD_START=2026-08-28 过滤逻辑与数据长度无关

### 2.7 宏观日历回填（8.2）
- `backfill_macro_events(start='2023-01-01')`：约 940 天 × 3.5s ≈ 55 分钟，后台任务执行
- 幂等（UNIQUE 键 + 已采日期跳过），可断点续采；完成后 macro_events 覆盖 2023-01 ~ 今日
- 模块 6 研究重算**不在本模块**——按前向等待期计划归模块 9（与 FDR 校正一起做，避免二次重算）

## 三、表结构变更

```sql
ALTER TABLE passive_signals ADD COLUMN source TEXT DEFAULT 'live';
-- signal_effect_report 每次重建, 新增列:
--   caliber TEXT  -- '合并'/'replay'/'live' (passive 三口径; active 仅'合并')
```

## 四、交付物清单

| # | 交付物 | 说明 |
|---|--------|------|
| 1 | quant_data.py 模块8节 | M8 常量、backfill_quotes_full、replay_passive_signals、compute_passive_signal_stats(source)、save_signal_effect_report(caliber)、validate_rsi24_timesplit、get_backfill_status |
| 2 | detect_signals 改造 | 增加 source 参数（默认 'live'，回放传 'replay'），幂等键不变 |
| 3 | dashboard.py | Tab 1 数据采集新增「历史回填」区（行情/宏观回填按钮+耗时提示）；Tab 3 新增口径切换 + RSI24 时间分割验证区块 |
| 4 | test_module8.py | 回填覆盖/幂等/双口径一致性/时间分割/扩窗回测 全链路测试 |
| 5 | 数据 | quotes ~4900 行（2020 起）；replay 信号（预计 3000+）；宽表全窗口；signal_effect_report 三口径；backtest 新批次；macro_events 2023-01 起 |

## 五、执行顺序

1. 备份 DB → `quant_data_backup_20260904_pre_m8_backfill.db`
2. 代码实现（dev 目录）→ 同步 Desktop
3. 行情回填（~1 分钟）→ 交界月份比对 → 指标全量重算 → 回放信号（幂等验证跑两遍）→ 宽表重建
4. 模块 3 三口径重算 + RSI24 时间分割验证（按 2.5 预注册规则出结论）
5. 模块 5 扩窗重跑（新 run_date 批次）
6. 启动宏观日历后台回填（55 分钟，与文档/测试并行）
7. 测试更新期望值 + 浏览器验证 + 双目录同步 + GitHub 提交
8. 问题日志 + SKILL 更新备份 + 项目记忆

## 六、风险与对策

| 风险 | 对策 |
|------|------|
| 前复权基准移动导致历史价格变化 | 全序列统一口径（正确行为）；交界月份抽样比对连续性；模块 3/5 全量重算 |
| 回放与实采口径差异污染结论 | source 列 + 三口径统计强制分离；背离时 live 定方向、合并定资格 |
| 宏观源深度不足（2023 前） | 已探测确认，目标修正为 2023-01 并诚实记录 |
| 旧 pe/pb/市值列在回填段为 0 | akshare *_daily 接口不提供估值字段；PE/PB 因子本就是路线图最低优先级，记录在案 |
| 回测 full 段拉长后 B1/B2 基准变化 | 同窗口重算（代码既有逻辑），oos 段不受影响 |
| SQLite 长事务与宏观后台回填锁冲突 | 宏观回填安排在本地重算全部完成之后启动；日级写入短事务 + 失败重试 |

## 七、问题日志

（实施过程中追加，五要素：现象/如何发现/根因/解决/预防）

### #1 模块5旧批次被扩窗重跑清除（2026-09-04）
- **现象**：`run_d2_backtests()` 扩窗重跑后，`backtest_runs` 只剩 2026-09-04 批次，2026-09-03 冻结批次消失，与计划 2.6「旧批次保留作对照」矛盾
- **如何发现**：`_m8_run_m5.py` 的批次汇总查询（GROUP BY run_date）只返回一个批次
- **根因**：`create_backtest_tables()` 每次调用先 `DROP TABLE IF EXISTS` 整批重建4张回测表（模块5既有设计，模块4同款模式）；开发计划撰写时未核对该建表函数的实际生命周期行为，"保留旧批"只是想当然
- **解决**：① 9/3 冻结批次确认完好保存在 Desktop 备份库（`quant_data_backup_20260903_pre_cap_repair.db` / `_pre_earnings_clean.db`，各 40 runs），需要对照时可恢复；② 给 `run_d2_backtests()` 增加同日批次幂等（同日重跑先清同日旧批再入库），防止同日重复批次叠账；③ 9/3 批次关键数字已在项目记忆与审计基线中留档
- **预防**：撰写涉及表生命周期的计划前，先读建表/写入函数确认 DROP/追加行为；"多批次共存"需求必须显式改造为追加式表结构（或入库前导出），不能依赖默认行为

### #2 oos 段起点断言写错（2026-09-04，测试自纠）
- **现象**：test_module8.py [9] 断言 oos 起点 = '2026-05-01' 失败，实际为 2026-05-04
- **如何发现**：模块8测试首跑 27/28，该项 ✗
- **根因**：知识截止日是**日期过滤器**（trade_date > 2026-05-01），段起点取其后**首个交易日**（2026-05-01 为周五，05-04 周一）；断言把过滤日当成了段首日
- **解决**：断言改为 `oos_start > '2026-05-01'`（与 test_module5 [3] 同口径）；重跑 28/28 通过
- **预防**：「截止日/起点日」类断言先确认字段语义是过滤边界还是实际首行；跨市场日历下首个交易日 ≠ 日历次日

## 八、执行结果（2026-09-04 交付）

| 步骤 | 结果 |
|------|------|
| 行情回填 | 三股超额覆盖：300750 自 2018-06-11（2001行）/ 00700 自 2016-09-29（2443行）/ AAPL 自 2016-12-14（2443行）；交界段（2025-02~04）与备份库比对：旧日期全保留、日收益率最大偏差 0.000000 |
| 指标重算 | 6887 行全窗口（2016-09-29 起） |
| 信号回放 | replay 3283 条（2021-01-05 ~ 2025-10-02）；live 792 条原样保留；二次回放零新增（幂等通过）；业务键零重复 |
| 宽表重建 | 6083 行（2018-06-18 ~ 2026-09-04） |
| 模块3三口径 | 合并 14 组 4075 样本 / replay 3283 / live 792；signal_effect_report 三口径各 56 行 |
| RSI24 时间分割（预注册裁决） | rsi24_oversold：n=179，分割点 2024-03-15，段A 超额胜率 -0.01pp/超额收益 -0.68%，段B +14.6pp/+2.91% → **继续观察**（开发段超额≈0，历史优势集中在近端段）；rsi24_overbought：n=360，段A -2.71pp/+0.36%，段B +0.41pp/+0.32% → **继续观察**。均未达转正也未触发淘汰，S1e 转正继续等 forward test 并行证据 |
| 有效池重排（诚实结果） | 4.6 年样本下原「有效」信号全部降级：RSI14超卖 超额-3.8pp、KDJ金叉 -5.5pp、MACD死叉(反向) +2.5pp 但胜率<55% → 候选；唯一存活：**BOLL突破上轨**（n=128，10日胜率 60.3%，超额 +5.1pp；replay 59.2% / live 64.3% 双口径同向）。13.5 个月样本期的「有效」评级为 regime 特定产物 |
| 模块5扩窗重跑 | 新批次 2026-09-04：full 段 2018-06-18~2026-09-04（2136日，B1 +607.6%/B2 +61.2%），oos 段 2026-05-04~（89日，B1 -3.3%）；oos S1a +10.2%(SR 3.33)/S2 +7.7%/S1e +7.4%，S1d -1.4%（继续观察名单）；冻结参数原样，未做任何调整 |
| 宏观日历回填 | 后台执行（目标 2023-01-01，约 55 分钟，幂等断点续采） |
| 测试 | test_module8.py 28/28；test_module5.py / test_module3.py 回归通过（对账限定最新批次） |
