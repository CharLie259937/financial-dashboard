# 槽位扩容变体预注册（S2-K6 · 2026-09-19 冻结）

> 起因：用户质疑 K=3 固定槽位是否过于死板。K 敏感性研究（2026-09-19，K=1→8 扫描，full 段费率 0.1%）实证：S2 满仓拒单率 **51%**（1198 触发拒 616）且 K=3 恰为 S2 夏普谷底（0.42 vs K=6 的 0.66）；回撤随 K 单调改善（−60.6%→−40.4%）。**但总收益的 K 曲线非单调且被事后股污染（S1a 的 K=2 峰值实质是给智谱行情加权，S1 研究已证伪该归因）——本研究只采用稳健事实（拒单率/回撤/夏普方向），明确拒绝把收益曲线作为选 K 依据。**
> 定位：本文档为预注册文档，变体定义与裁决规则在任何 S2K6 回测/前向样本产生之前冻结（git commit 时间戳为凭）。与门控变体（S*M/SxE/SxV）同模式：原版不动、只加变体、独立裁决。
> 对照设计：S2K6（扩容）与 S2V（波动域滤波，拒单率已降至 3%）构成同一拥挤问题的两种解的**并行对照**——前向将回答"高密度信号族该滤掉一半触发，还是让全部进来但摊薄仓位"。

## 一、变体定义（冻结）

- **S2K6 = S2（组合·超跌反弹·持有5日）+ 并发槽位 K=6**
- 触发集 / 持有期 / 费率 / 出场规则 / T+1 纪律与 S2 **完全一致**；唯一差异 = 并发槽位数 6（每槽 1/6 资金独立复利，vs 原版每槽 1/3）
- 宿主选择依据：S2 是拒单最重的策略（51%）；S1b 的 K=3 恰为全局最优（14% 拒单，不动）；S1a 的 K 敏感度被事后股污染且夏普 K=2–6 平坦（0.92–1.00，无稳健差异）——**只对 S2 注册**

## 二、机制对照预期（预声明，非承诺；全部标注选择泄漏）

- 拒单率：51% → 约 22%（K=6 扫描实测值），成交笔数约 +47%
- 回撤：温和改善方向（扫描显示单调）
- 单笔权重减半 → 复利稀释：总收益方向**不做预测**（这正是要由对照回答的）

## 三、裁决规则（冻结，不得依前向结果修改）

- 门槛：S2K6 完成 **≥20 笔**前向交易后裁决（FORWARD_ADJ_MIN_TRADES 同款）
- 对照：S2K6 vs S2 前向并行对照（同窗口、同池、同协议），另报超额 vs B1
- 判定（四档，与门控变体同构）：超额 vs B1 ≤0 → 淘汰建议；>0 但 ≤原版 → 无增量淘汰；>0 且 >原版且胜率 ≥原版 → 转正候选；其余继续观察
- 计数起点：引擎接入交付当日登记（`forward_meta.slot_protocol` 独立键，DELETE+INSERT 幂等）
- 样本口径注意（预声明）：K6 与 K3 的前向交易**触发集相同**（同信号不同仓位容量），完成笔数会不同（K3 拒掉的触发在 K6 成交）——并行对照按各自完成交易统计，窗口起点一致

## 四、树杈纪律（资金维度扩展约束）

1. 资金维度**一次只裁决一个变体**：波动率倒数槽位（S2VW，方向四/优化清单 ATR N 值）与 regime 条件 K 等 S2K6 出前向裁决后另行预注册
2. 不做动态 K / 按信号质量排序入场（无据先验 + 多重检验负担）
3. S2VK6 类叠加变体属第三层，须两片叶子各自裁决后另立预注册

## 五、冻结协议合规声明

1. 不修改 S2 原版定义与任何现行策略的槽位（BT_MAX_POSITIONS 全局默认仍为 3，仅 S2K6 通过 spec 级 max_pos 覆盖——引擎新参数向后兼容，原版行为逐位不变）
2. 不触碰前向既有策略的重放确定性（S2K6 为新增前向序列，fwd_start 起步）
3. 回测段 S2K6 结果一律机制对照展示（K=6 的选择来自 2026-09-19 样本内扫描，选择泄漏明示）；唯一业绩证据 = 前向
4. K 扫描的总收益读数（含 S1a K=2 峰）禁止作为任何资金配置依据引用

## 六、验证计划

- 引擎硬不变量：S2K6 任一时刻并发持仓 ≤6；S2 原版（及全部既有策略）在引擎改动前后行为逐位一致
- fwd_start 纪律：无起点前前向交易
- 机制对照批次入多批次共存表；全量回归 + AppTest 全页 0 异常
