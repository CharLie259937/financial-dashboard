# 模块 1→5 逻辑关系与功能实现总览

> 项目：金融量化分析看板（financial_dashboard）
> 范围：前置阶段（采集/看板）+ 模块 1-5 全链路
> 状态：模块 1-4 已完成验收；模块 5 D1（回测引擎）已完成，D2（入库/敏感性）/D3（看板 Tab 8）进行中

---

## 一、一句话逻辑主线

五个模块构成一条「**原始数据 → 可信数据 → 特征底座 → 统计验证 → 独立影响 → 执行验证**」的流水线。每个模块回答一个递进问题，后一模块以前一模块的产出为输入，对照基准逐层收紧：

| 模块 | 回答的核心问题 | 对照基准 |
|------|--------------|---------|
| 模块1 数据治理 | 数据能不能用？ | —（治理，无基准） |
| 模块2 技术因子 | 有没有标准化特征？ | —（因子计算） |
| 模块3 胜率统计 | 信号**统计上**有效吗？ | 随机买入基准 |
| 模块4 事件研究 | 事件的**独立**影响是多少？ | 大盘指数（市场模型） |
| 模块5 策略回测 | 按信号**实际交易**赚钱吗？ | 双基准：买入持有 + 大盘指数 |

---

## 二、数据血缘图

```
[前置阶段A/B] 行情采集 + 主动/被动埋点采集 + 实时看板
        ↓
[原始层] daily_quotes / passive_signals / active_events
        ↓
[模块1] 时间锚定 + 清洗 + 哑变量展开
        ↓ daily_feature_base（宽表29列）
        ↓
[模块2] 11类技术因子滚动计算 ──→ daily_indicators
        │                            │
        │   （因子同时驱动 detect_signals 生成被动信号，闭环）
        ↓                            ↓
[模块3] 信号胜率/盈亏比/超额统计（vs 随机基准）
        ↓ signal_effect_report + 有效信号池（4组达标）
        │
        │   [模块4] benchmark_index 采集（沪深300/HSI/标普500）
        │        ↓
        ├────→ [模块4] 事件研究：α/β回归 + AR/CAR + t检验
        │        ↓ event_study_* 4张表
        │        │
        ↓        ↓
[模块5] 策略回测：S1a-d/S2/S3 策略 + B1买入持有 + B2指数基准
        ↓ backtest_* 4张表（D2）+ 看板Tab8（D3）
```

---

## 三、前置阶段：采集与看板基础（模块 1 之前的地基）

| 能力 | 实现要点 |
|------|---------|
| 三市场实时查询 | 新浪 hq 接口，美股夏令时时段判断，K线+大盘指数条 |
| 长期追踪 | 每股**独立阈值**（涨跌幅%/量能倍数），后台线程持续采集（规避标签休眠断连） |
| 股票池管理 | 增删/活跃切换/独立阈值，多股逗号分隔输入 |
| 日线采集 | akshare 前复权为主，新浪K线兜底 |
| 主动埋点采集 | 财报日程 + 分红明细，自动/手动事件区分 source_type |

产出三张核心表：`daily_quotes`、`passive_signals`、`active_events`——模块 1-5 的全部数据源头。

---

## 四、逐模块功能详解

### 模块 1：数据盘点、清洗与时间对齐（宽表打底）

| 项 | 内容 |
|----|------|
| 解决的问题 | 多源数据（行情/被动/主动）口径不一、无法关联 |
| 输入 | daily_quotes + passive_signals + active_events |
| 核心规则 | ① 时间锚定：盘中信号→当日，**盘后事件→下一交易日**；② 同日多信号按类型生成**独立 0/1 哑变量**，不合并；③ 前复权、停牌标记 |
| 产出 | `daily_feature_base`（29列：维度+行情+sig_*+evt_*） |
| 关键函数 | generate_wide_table / batch_generate_wide_tables |
| 下游依赖 | 模块2因子源、模块3对齐依据、模块5回测行情源 |

### 模块 2：基础技术因子（特征底座）

| 项 | 内容 |
|----|------|
| 解决的问题 | 信号对比缺基准，建模缺特征 |
| 输入 | daily_feature_base 行情 |
| 指标集（11类） | 趋势：MA5/10/20/60、MACD三值；震荡：RSI(14)、KDJ(9,3,3)、BOLL三轨；波动：20日波动率、ATR(14)；量能：5/20日均量、OBV；收益：当日/3/5/20日涨跌幅 |
| 产出 | `daily_indicators` 表 |
| 关键函数 | calc_factors / calc_all_indicators / generate_indicators |
| 隐藏角色 | **因子表同时是被动埋点的检测引擎**（detect_signals 基于因子值生成 passive_signals，形成闭环） |
| 约束 | 全部滚动计算无未来函数；初始窗口空值保留不填充 |

### 模块 3：埋点信号历史胜率与收益统计

| 项 | 内容 |
|----|------|
| 解决的问题 | 每类信号的实际预测效果如何，哪些值得用 |
| 输入 | passive_signals + active_events + 行情（未来收益，仅用于回测） |
| 统一口径 | 4个持有期（1/3/5/10日）；收益=(未来第N日收盘−触发日收盘)/触发日收盘 |
| **关键设计** | **随机基准**：全量任意交易日买入持有N日的胜率与收益 → 超额胜率/超额收益（防止牛市里什么信号都显得准） |
| 分组维度 | 被动：类型×子类型×方向；主动：类型×方向×等级三维交叉 + 盘前/盘中/盘后 |
| 产出 | `signal_effect_report` 表 + 有效信号池（入选标准：胜率>55% & 盈亏比>1.2 & n≥30） |
| 增强功能 | 投资组合标注（每股数据天数/累计收益/年化/波动/趋势画像）+ 股票表现差异分析（信号适应性、信号×股票超额胜率热力图、趋势归因） |
| 实测结论 | 严格达标仅 **4 组且全是超跌反弹类**（大跌10日盈亏比2.03最强、跌破下轨5日、大跌5日、金叉3日）；earnings 事件最强（胜率66-83%） |
| 下游依赖 | 模块5的策略选型依据 |

### 模块 4：事件研究法（Event Study）

| 项 | 内容 |
|----|------|
| 解决的问题 | 模块3的高收益可能只是大盘涨——剔除大盘，度量事件**独立**影响 |
| 输入 | active_events + daily_quotes + **benchmark_index（本模块新增采集）** |
| 方法 | 估计窗[-120,-10]一元回归 个股收益=α+β×指数收益；事件窗[-5,+10]计算 AR/CAR；6窗口单样本 t 检验 |
| 产出 | `benchmark_index`（沪深300 5981条/HSI 3204条/标普500 5702条）+ `event_study_results/curves/events/meta` 4张表 |
| 关键函数 | collect_benchmark_index / run_event_study / save_event_study_results / load_event_study_from_db |
| 样本治理 | 同日同类型事件去重；过滤早于行情起点/未来事件；估计窗最少60观测 |
| 与模块3的差别 | 模块3看**绝对收益**（信号日收盘买），模块4看**超额收益**（剔除大盘后的 CAR） |
| 实测结论 | earnings 事件日 CAR +2.27%（p=0.095 弱显著*），后10日 +3.66%（n=3 样本小）；dividend 未通过显著性检验 |
| 下游依赖 | benchmark_index 被模块5复用为 B2 指数基准；S3 事件策略的依据 |

### 模块 5：信号策略回测与基准对比（进行中）

| 项 | 内容 |
|----|------|
| 解决的问题 | 统计有效 ≠ 策略可赚钱——T+1成交滞后、资金占用、信号密度都是损耗；同时补齐第一阶段验收第4条（双基准线） |
| 输入 | passive_signals（**DISTINCT 日级去重，保留方向**——宽表 sig_* 合并了方向不可用于方向性回测）+ active_events + 宽表行情 + benchmark_index |
| 引擎设计 | 槽位制资金模型（K=3 槽各 1/K 独立复利）；**T日收盘确认 → T+1开盘成交 → 持有N个交易日 → 开盘卖出**；满仓拒绝/末端放弃计数 |
| 策略集 | S1a-S1d（4组达标单信号各自回测）+ S2（超跌反弹组合 OR 增样）+ S3（earnings事件持有10日） |
| 双基准 | B1 买入持有等权（各股自身首日开盘建仓）+ B2 大盘指数等权（三市场独立复利，休市贡献0） |
| 特色指标 | **跳空成本** = 回测收益 − 模块3统计收益（量化"理论有效"与"实际执行"的差距） |
| 跨市场日历 | 三市场交易日并集为净值日历；个股休市沿用前值；节假日对齐已验证（感恩节等抽查通过） |
| 产出（计划） | `backtest_runs/equity/trades/meta` 4张表 + 看板 Tab8「策略回测」 |
| D1 实测 | 单元测试4/4（人工样本净值误差<1e-12）；S1a 大跌持有10日 **+14.13%（夏普1.85）跑赢双基准**（B1 +9.86%/B2 +10.38%）；S2 组合 +17.88% 但 272 触发仅成交 120（资金容量瓶颈）；S3 仅4笔样本跑输 |

---

## 五、模块间的三个关键逻辑关系

### 5.1 验证口径的三段演进（本项目最核心的分析逻辑）

| 层 | 口径 | 回答 | 交易假设 |
|----|------|------|---------|
| 模块3 | 统计口径 | 信号理论上有效吗 | 信号日收盘买入，无资金约束、无成本 |
| 模块4 | 独立影响 | 是信号的功劳还是大盘 | 剔除大盘（市场模型），只看超额 |
| 模块5 | 执行口径 | 实盘能赚钱吗 | T+1开盘成交 + 槽位资金约束 + 可选费率 |

三个口径的**差值本身是结论**：模块5−模块3 = 跳空成本；模块3−模块4 ≈ 大盘贡献。

### 5.2 benchmark_index 的双重角色
模块4为市场模型回归而采集 → 模块5直接复用为 B2 指数基准。一次采集，两处消费。

### 5.3 passive_signals 的双重角色
- 模块3：统计样本（全量，按类型×方向分组）
- 模块5：回测触发器（**日级去重后**——原始表日均5.5条含日内重复，直接用会样本虚增）

---

## 六、贯穿全程的工程红线

1. **无未来数据**：盘后事件→次日；因子滚动计算；未来收益只出现在"效果统计/回测"，绝不进入特征输入
2. **主键统一**：股票代码+交易日，全库关联口径一致
3. **同日多信号**：0/1 哑变量分别记录，不合并
4. **小样本警示**：n<30 仅作参考（S3 事件策略 4 笔，结论必须标注）
5. **异常打印**：禁止静默失败
6. **结果可回读**：完整结果 JSON 入 meta 表，看板重启不丢（模块4/5 模式）

---

## 七、当前完成状态

| 模块 | 状态 | 数据表 | 看板入口 |
|------|------|-------|---------|
| 前置 | ✅ | stock_pool / daily_quotes / passive_signals / active_events | 实时查询 / 长期追踪 |
| 模块1 | ✅ | daily_feature_base | 量化分析·数据概览 |
| 模块2 | ✅ | daily_indicators | 量化分析·技术因子 |
| 模块3 | ✅ | signal_effect_report | 量化分析·信号效果 |
| 模块4 | ✅ | benchmark_index + event_study_*×4 | 量化分析·事件研究 |
| 模块5 | 🔶 D1完成 | backtest_*×4（D2） | Tab8 策略回测（D3） |

**下一步**：模块5 D2（批量入库+费率敏感性+分段稳健性）→ D3（看板Tab8+浏览器验证）→ 第一阶段四条验收标准全部闭环，进入第二阶段建模。
