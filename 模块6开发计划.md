# 模块 6 开发计划：宏观经济日历采集与事件研究

> 承接关系：模块 5（策略回测）完成 → 模块 6 是第一阶段验收后的第一个增量模块
> 数据源依据：`经济日历数据源审计.md`（2026-09-02，5 轮探测 ~25 采样日）
> 方法论依据：`financial-quant-dashboard-dev` skill（五步闭环 / 无未来数据纪律 / 踩坑经验库 #15 #16）
> 启动日期：2026-09-03 | 预计工期：1-2 天（数据基础已在审计阶段验货）

---

## 一、模块定位与目标

### 1.1 为什么做宏观事件
- 2026-09-02 评估 dailyfx/IG 日历被否（403 无 API、历史仅到昨天），审计确认百度股市通 sapi 可用
- 模块 3/4/5 全部是个股/组合层面；宏观事件（FOMC/非农/CPI）是三市场共同驱动因子，缺失它意味着
  看板无法回答"昨天为什么跌"这类最常见的问题
- 模块 4 的事件研究法（AR/CAR）可以直接升级为宏观版：标的从个股换成指数，事件从财报换成宏观发布

### 1.2 模块目标
1. 宏观经济日历入库：全市场事件（20 地区），可回溯至模块 5 回测窗口起点 2025-07-29
2. 事件研究（宏观版）：高重要性事件 × 三市场指数，常数均值模型 AR/CAR + t 检验
3. 意外方向拆分：公布值 vs 预期值 → 高于/低于预期分组（宏观研究的核心增量，纯"事件后涨跌"无方向语义）
4. 看板 Tab 9：日历浏览器 + 采集状态 + 研究结果可视化

### 1.3 明确不做（边界）
- 不做宏观事件的**策略回测**（模块 5 引擎是多空持仓模型，宏观事件先以研究结论形式沉淀，回测属后续独立决策）
- 不采集 dailyfx/IG（已否决）；不用 akshare 封装（踩坑 #16）
- 不做事件预注册假设检验之外的"挖矿"：家族×指数×方向全量计算后按 n 和显著性如实呈现，不据此调参
- 财报披露日源（sapi cate=report_time，S3 复活钥匙）**本模块不动**，留作独立任务（见审计报告第四节）

---

## 二、数据源审计结论（2026-09-02，已归档）

| 项 | 结论 |
|----|------|
| 接口 | `finance.pae.baidu.com/sapi/v1/financecalendar`，cate=economic_data |
| 字段 | date/time(北京时间)/region/title/star(仅1/2级)/pubVal/indicateVal/formerVal |
| 质量 | 历史实际值填充 100%；按日幂等；无数据洞；NFP 锚定命中 |
| akshare 不可用原因 | 默认日期硬编码 "20251126"；分页请求缺指纹 → 事件数>100 的日期 WAF 403 |
| 正确姿势 | 自写客户端：curl_cffi impersonate="chrome110" 全请求 + 手动分页；**无需 cookie**（2026-09-03 补充实测，见问题日志 #2） |

---

## 三、总体设计

### 3.1 数据库新表（沿用模块 4 "结果可回读"模式）
```sql
macro_events          -- 原始事件: trade_date/time/region/title/star/pub_val/forecast_val/
                      -- former_val, UNIQUE(trade_date,time,region,title) 幂等键
macro_study_results   -- 分组统计: group_id(家族|地区|指数|方向) × 窗口 CAR/t/p/sig
macro_study_curves    -- 分组曲线: group_id × 相对日 AAR/CAAR
macro_study_events    -- 事件明细: 每事件×每指数的交易日映射结果+纳入标志+排除原因
macro_study_meta      -- 运行参数与诚实性声明
```

### 3.2 事件 → 交易日映射规则（跨时区红线）
事件时刻为**北京时间**，映射到目标市场的交易日：
- 美股：北京→ET 换算（夏令时差 12h，冬令时差 13h；**冬令时北京 00:30 = 前一日 11:30 ET，跨日陷阱**），
  ET 时刻 ≤ 16:00（收盘）→ 当日，否则次日；再吸附到 .INX 交易日历
- A 股：北京时刻 ≤ 15:00 → 当日，否则次日；吸附到沪深300日历
- 港股：北京时刻 ≤ 16:00 → 当日，否则次日；吸附到 HSI 日历
- 典型锚点：非农（北京周五 20:30）→ 美股当日、A 股下周一；FOMC（北京周三 02:00）→ 美股前一日、A 股当日

### 3.3 事件研究口径（与模块 4 的差异点）
| 项 | 模块 4（财报事件） | 模块 6（宏观事件） |
|----|-------------------|-------------------|
| 标的 | 个股 | 指数（.INX/HSI/sh000300） |
| AR 模型 | 市场模型（个股=α+β×指数） | **常数均值模型**（AR=指数收益−估计窗均值，标的自身即市场） |
| 估计窗/事件窗 | [-120,-10] min60 / [-5,+10] | 相同（复用常量与 _agg_group/_t_test） |
| 分组 | event_type | **家族（关键词分类）× 地区 × 指数 × 意外方向** |
| 溢出视角 | — | 美国事件同时研究对三市场的影响；中国事件研究 A+H |

### 3.4 家族分类与意外方向
- 18 条关键词规则（顺序敏感）：核心CPI 先于 CPI、非农/ADP 归"就业报告"等；未命中 → '其他'（仅留明细不进统计）
- 方向：公布/预期均可数值解析时拆 高于预期/低于预期；解析失败仅入"全部"组
- 汇总组"全部高重要性"按 地区×指数 并行计算（含方向拆分）

### 3.5 采集运维
- 回填：2025-07-29 ~ 今日（与回测窗口对齐），幂等断点续采，日间节流 2.5s
- 日常：collect_macro_events(最近3天)——最近 3 天强制重采（实际值陆续补填），其余日期已有即跳过
- 上限防御：单日最多翻 6 页（600 条）

---

## 四、任务清单

- [x] M6.1 数据源审计（2026-09-02 完成，独立报告）
- [x] M6.2 表结构 + 自写采集器（指纹+分页+幂等，cookie 链路经调试删除，见问题日志 #1）
- [x] M6.3 全窗口回填（2025-07-29 ~ 2026-09-03 完成，24923 行；期间发生"疑似中断"误判与双进程并行，见问题日志 #3）
- [x] M6.4 交易日映射（DST 感知）+ 家族分类 + 数值解析（冒烟验证通过）
- [x] M6.5 宏观事件研究引擎（常数均值 AR + 复用模块4聚合/t检验 + 结果入库）
- [x] M6.6 看板 Tab 9（日历浏览器/采集状态/研究结果可视化；浏览器验证待回填后进行）
- [x] M6.7 test_module6.py 全链路验证（64/64 通过：映射锚点13/家族15/数值10/方向7/AR5/完整性6/幂等1/研究对账7）
- [x] M6.8 SKILL.md 模块编号 + 数据源表更新 + 1.5 问题日志协议（2026-09-03 用户确立）
- [x] M6.9 双目录同步（2026-09-03 补齐并字节级校验，见问题日志 #4）+ 浏览器自检 9/9 通过（零红色报错，2026-09-03 01:05）| 用户验收：进行中

---

## 五、验收标准

- [ ] macro_events 覆盖 2025-07-29~今 无缺口（周六日允许 0 行，工作日 0 行须复核）
- [ ] 重复采集幂等（同日重采行数不变）
- [ ] 交易日映射锚点全对（非农/FOMC/中国事件/冬令时跨日）
- [ ] 研究结果表非空且方向拆分组存在；CAR[0,0] 等于该组 AR[0] 均值（口径自洽）
- [ ] 看板 Tab 9 渲染无红色异常；用户浏览器验收通过
- [x] 计划文档含问题日志（见下），每次错误修复即时追加

---

## 六、问题日志（每次错误修复必须记录：现象→发现→根因→解决→预防）

> 规则（2026-09-03 用户确立，写入 SKILL.md）：以后每一次遇到错误并修复，都必须在本节追加记录，
> 包含五个要素——问题现象、如何发现、根因、解决方案、预防措施。供以后读取反思。

### #1 内联 cookie 逻辑失败：hm.js 不返回 HMACCOUNT
- **现象**：模块 6 采集器首次运行抛 `ConnectionError: hm.js 未返回 HMACCOUNT cookie`
- **如何发现**：3 天冒烟测试立即触发；用独立调试脚本对比 akshare 原版（成功）与内联版（r2 返回 200 但 cookies 为空）逐步定位
- **根因**：hm.baidu.com 只对带特定 referer/origin 头的请求下发 HMACCOUNT；akshare 原版恰好把 sapi 请求头带进了 session，内联版用了不同的 headers。更深一层：**cookie 链路根本是非必要的**
- **解决**：逐步降级测试发现 sapi 裸请求（无任何 cookie）即返回 200 全量数据，>100 条日期的翻页同样正常（101/101、115/115 完整）→ 删除整个 cookie 获取/缓存代码，采集器只保留浏览器指纹
- **预防**：依赖第三方"最佳实践"前先做最小化实验（本例一个"无 cookie 请求"就能省掉 60 行代码）；调试外部接口时按"请求要素逐个去掉"的顺序找真正必需项

### #2 测试脚本自身的期望值写错（非代码 bug）
- **现象**：冒烟测试两处映射结果与注释期望不符（非农→CN 给出 08-01 而非"期望 08-03"；冬令时 00:30→US 给出 2026-01-02 而非"期望 2025-12-31"）
- **如何发现**：人工复核测试日历构造——fake 日历里根本没有 08-03；00:30−13h=**01-01** 11:30（我在注释里心算成 12-31），且 01-01 是美股休市日，吸附到 01-02 恰恰正确
- **根因**：期望值凭心算写死，且假日历与期望不一致；被测代码逻辑正确
- **解决**：确认代码行为正确，不改代码；临时脚本随用随删
- **预防**：时区跨日的心算必须落在纸面（列出北京时刻−时差=ET 时刻的完整算式）再写期望；测试日历必须包含期望日期本身，否则"吸附"语义会制造假失败

### #3 后台回填"疑似中断"误判（进程活着，句柄与日志双双误导）
- **现象**：恢复会话后查后台回填任务返回 task not found，任务输出日志文件为空；查库显示 macro_events 只覆盖到 2025-12-17，2025-12-18 之后缺失 178 个工作日——三条证据都指向"回填已死"
- **如何发现**：三步递进——①TaskOutput 非阻塞查询报 task not found；②读任务输出日志为空；③查库按月分布见大缺口。据此判定"进程已死"并启动了续采脚本。**事后发现均为误判**：任务完成通知最终到达（原任务 exit 0 正常结束），且进程表里原进程 PID 30648 从 0:38 起一直健康推进，与续采进程并行跑完
- **根因**（三层，一层比一层深）：
  ①任务句柄注册表跨会话不保留——句柄查不到 ≠ 进程死亡；
  ②Python stdout 在重定向到文件时是**全缓冲**（非行缓冲），日志行滞留缓冲区未落盘，输出文件为空 ≠ 无输出；
  ③DB 只到 2025-12-17 是**正常进度**而非中断——原进程 0:38 启动、约 15 天/分钟推进，查库时刻它恰好走到 12 月中旬
- **解决**：续采进程重启后与原进程并行写库，幂等键（UNIQUE + INSERT OR REPLACE）兜底，最终 0 重复、数据完整到 2026-09-03；两个进程均 exit 0
- **预防**：①判断后台进程死活的第一手段是**查进程表**（Get-CimInstance Win32_Process 按 CommandLine 过滤），任务句柄和输出日志只能作辅助证据；②Python 长任务必须 flush=True 逐行落盘（或 python -u），否则"日志为空"毫无诊断价值；③对"进行中"的长任务，DB 行数增速是唯一可信的进度证据——先算预期速率再下结论（本例 6 分钟 × 15 天/分钟 ≈ 90 天 ≈ 恰好走到 12 月中旬，算一下就不会误判）；④幂等设计在本案从"保险"变成"刚需"——双进程并行写库零损坏，长任务幂等是硬要求

### #4 双目录同步声明与实况不符（Tab 9 未真正到桌面）
- **现象**：上一会话末尾记录称模块 6 文件"已同步到桌面"，实况是桌面 dashboard.py 仍为旧版 141508 字节（缺 Tab 9），模块6开发计划.md / 经济日历数据源审计.md / audit_macro_calendar.py 在桌面缺失——若当时直接启动看板，用户会看到 8 个标签页且无宏观模块
- **如何发现**：恢复会话后用 Select-String 在桌面 dashboard.py 搜 quant_tab9 无命中；再比对两目录 dashboard.py 大小（155575 vs 141508）与时间戳坐实
- **根因**：同步动作在上一会话末尾被打断（与 #3 同源），而流程上"复制命令执行成功"被当成了"同步已完成"，缺少同步后的校验闭环
- **解决**：重新复制 4 个模块 6 文件到桌面，并逐个核对字节数与源目录一致
- **预防**：同步后的验收 = 文件大小（和修改时间）比对，不是复制命令的返回；"已同步"结论只能由校验结果产生；会话末尾的长动作（回填/同步）做完必须立刻校验，不留"回来再看"的尾巴

### #5 分组键含 None 导致 sorted() 崩溃（全量数据才触发的隐性 bug）
- **现象**：run_macro_study() 抛 `TypeError: '<' not supported between instances of 'str' and 'NoneType'`，研究引擎在数据入库后、聚合排序一步崩溃
- **如何发现**：回填完成 → 用小样本（此前只有半窗数据时研究曾冒烟跑通）之外的全量数据首次正式运行研究；报错栈直接指向 `sorted(groups.items())`
- **根因**：分组键 = (地区, 家族, 指数, **意外方向**)，方向可为 None（公布/预期不可解析时）。半窗冒烟时恰好每个 (地区,家族,指数) 只出现过"全 None"或"全有值"一种形态，tuple 比较未跨 None/str；全量数据下同一前缀的两组（direction=None 与 direction='高于预期'）需要比较第 4 元素 → str vs None 崩溃
- **解决**：`sorted(groups.items(), key=lambda kv: tuple(str(x) for x in kv[0]))` 显式转字符串排序；修复后 5158 条事件 → 256 组统计正常入库
- **预防**：**含 None 字段的复合键永远不要裸 sorted()**（冒烟通过不代表全量通过——样本形态覆盖度不同）；写法上可统一用 `str(x)` 归一化键排序，或键内 None 落库前就替换为哨兵值（如 '全部'）

### #6 剔除股残留数据继续进入全部分析模块（股票池形同虚设，2026-09-03）
- **现象**：股票池已缩至 3 只（00700/300750/AAPL），但信号效果、事件研究、策略回测等所有分析栏目仍基于 5 股数据——被剔除的 09889/600000 继续进入随机基准、组合画像与回测组合（backtest_trades 池外残留 387 笔），污染全部统计结果
- **如何发现**：用户看板验收时发现（数据采集栏只有 3 只，后续栏目仍是 5 股口径）——**数据域不一致这类"数字看起来正常"的问题只有人肉对口径才能发现**
- **根因**（两层）：①`remove_from_pool` 只删 stock_pool 记录，不级联清理 5 张数据表（daily_quotes/passive_signals/active_events/daily_indicators/daily_feature_base），历史数据全部残留；②全部 14 处分析函数直接全表扫描个股数据表，**没有"当前池"这一单一事实源**——stock_pool 表只影响采集入口，不影响分析口径
- **解决**（三步）：①新增 `_pool_code_set()`/`_pool_sql_filter()` 作为池单一事实源（池空返回恒假条件 1=0），14 处数据访问函数全部注入池过滤（含事件研究的 active_events 查询与回测 `_collect_triggers` 事件查询两处隐蔽入口）；②`remove_from_pool` 改为级联删除 5 表数据并按表回报行数，看板移除按钮加级联警告 + session_state 跨 rerun 成功提示；③备份 DB 后清理存量残留 1686 行（5 表）+ 事件/回测结果表池外残留，重算模块3/4/5（事件研究 10 事件、回测 40 run/849 笔，明细代码=池内 3 股）
- **预防**：**分析口径的股票域永远从 stock_pool 派生，禁止全表直读个股数据表**；"移除=级联删数据，停用=is_active=0 保留数据不进分析"语义分离并在 UI 明示；池结构性变更后的固定收尾三件套=清理残留 → 重算全部结果表 → 跑测试对账

### #7 池过滤改造自身引入 UNION ALL 绑定数错误（f-string 拼接 SQL 的固有陷阱）
- **现象**：重启看板后数据概览统计抛 `sqlite3.ProgrammingError: Incorrect number of bindings supplied. The current statement uses 21, and there are 3 supplied`
- **如何发现**：重启 Streamlit 后读后台服务日志尾部发现 Traceback——改造后**必须重启并读日志**这一步抓到的
- **根因**：`get_feature_base_stats` 的 signal_breakdown 是 7 分支 UNION ALL，池过滤 `{pool_f}` 注入后占位符 ×7（3 股=21 个），但参数只传了一份 `pool_args`（3 个）；event_breakdown 同理 6 分支
- **解决**：参数列表乘以分支数（`pool_args * 7` / `pool_args * 6`）；并全量审计其余 13 处 `pool_f` 用法（均为单次出现，参数匹配）
- **预防**：把可复用过滤片段 `{pool_f}` 注入既有 SQL 前，先数该语句内占位符出现次数，参数列表乘同倍数；结构性代码改造后的验证动作=重启服务 + 读日志尾部 + 点开受影响页面（本次靠前两步拦截在用户看到之前）

### #8 池变更后测试期望值与函数签名过时（连带影响未同步）
- **现象**：重算后 test_d3_tab 7 项失败（交易明细 1119→849 行、S1a OOS 收益 10.48→10.23 等数值、"样本外最强"S1a→S2、费率脆弱名单 [S1d,S2]→[S1d]）；test_per_stock/test_portfolio_diff 抛 `ValueError: too many values to unpack (expected 2, got 3)`；test_per_stock 硬编码校验股 600000（已剔除）
- **如何发现**：重算完成 → 跑全部测试脚本对账（D3 24/31 失败明细直接列出全部差异）
- **根因**：①回测期望值是 5 股时代快照，池变更后未同步更新；②`compute_per_stock_signal_stats` 此前增加反向基准返回值（2→3 个）时漏改两个旧测试的解包；③测试硬编码个股代码而非从池动态取
- **解决**：期望值更新为 3 股池实测值并在 test_d3_tab 文件头注明"期望值基准: 2026-09-03 股票池 5→3 后"；两处解包改为 3 值；600000 校验改为动态取池内有 rsi_oversold 信号的股票；更新后 31/31 通过
- **预防**：数据域（池/窗口/口径）任何结构性变更后，"跑全部测试 + 更新期望值"是必做收尾；函数返回值个数变更必须同步 grep 所有调用方与测试脚本；测试中涉及个股的一律从池动态取，禁止硬编码代码

### #9 测试脚本相对路径连接在错误 cwd 静默新建空库
- **现象**：test_module4 入库验证段抛 `sqlite3.OperationalError: no such table: benchmark_index`，且编辑目录被"凭空"创建了一个 0 字节 quant_data.db
- **如何发现**：跑测试报错后检查报错行——`sqlite3.connect('quant_data.db')` 用相对路径，实际连接的是 shell 当前目录下新建的空库，而非桌面运行目录的真实库
- **根因**：测试脚本内嵌相对路径连接（早期在正确 cwd 手动运行的写法），不校验库文件存在与否；SQLite 默认行为是"不存在即新建"，错误被延迟到查表才暴露
- **解决**：删除误建空库，用 cwd=Desktop\financial_dashboard 重跑通过；此类脚本应统一走 `quant_data.get_db()`（与数据层同源、绝对路径）
- **预防**：测试脚本禁止裸 `sqlite3.connect('相对路径')`——要么 `sys.path.insert` 后用 `quant_data.get_db()`，要么显式绝对路径；连接前 `os.path.exists` 校验防"静默新建"；跑脚本时显式设 cwd

### #10 个股事件研究模块因财报日期数据不合格整体下线（2026-09-03，用户决策）
- **现象**：看板「🔬 事件研究」Tab 的统计建立在不可信数据上——10 个有效事件中 3 个财报事件的日期是**会计年度期末**（00700 标注"2025-12-31 会计年度12-31"，真实披露日在次年 3 月）；active_events 中 77 条财报事件全部为此类日期，且 00700 同日存在 3 条重复（非幂等残留）；与 S3 策略 2026-09-02 被删同根因
- **如何发现**：用户验收看板时主动质疑"事件研究 Tab 与宏观日历是否功能重叠"；随即核查 active_events / event_study_events 数据构成确认——①数据源不合格，②有效样本仅 10 个（7 分红 + 3 财报）分组后无统计意义，③与宏观日历**无数据重叠**（active_events 中 macro/policy 类型 0 行，宏观事件研究完全由 macro_events 24923 行承担，两者仅方法论同源、标的域不同：个股公司事件 vs 指数宏观事件）
- **根因**：财报事件采集时未审计日期语义——akshare 港股财报接口返回会计年度期末而非披露日；该缺陷在 S3 删除时已定性，但**当时只删了策略、未排查共用同一数据的事件研究 Tab**，同一脏数据继续供给另一个分析模块
- **解决**：整体下线模块4个股事件研究——①删 Tab（9→8，后续 Tab 重编号）；②删 quant_data.py 6 个专属函数共 296 行（_load_es_data/_aligned_returns/_event_study_one/_build_conclusions/save_event_study_results/load_event_study_from_db/run_event_study），**保留共享统计核心**（EVENT_WIN/EST_WIN/CAR_WINDOWS 常量、collect_benchmark_index、_t_test、_agg_group——宏观研究与回测 B2 依赖）；③删前备份 DB 后 DROP event_study_* 4 表；④删 test_module4.py；⑤基准指数采集 UI 从事件研究 Tab 迁至「数据采集」Tab；⑥验证：模块6测试 64/64、模块5测试 27/27、浏览器 8 Tab 逐项通过零红错
- **预防**：**数据源审计结论必须全链路执行**——一个模块因数据缺陷下线时，必须排查共用该数据源的所有兄弟模块（S3 删除时漏查事件研究 Tab 是本次的直接教训）；分析模块上线前核查输入样本量与日期语义；大块代码删除用带边界断言的脚本（本例断言拦截了 2 次行号漂移，写盘前中止零损失）

### #11 77 条错误财报事件的存量清理与采集入口停用（2026-09-03，#10 收尾）
- **现象**：#10 删除事件研究模块后，active_events 中 77 条错误日期财报事件仍在库中，继续污染三处下游——「事件管理」Tab 展示、信号效果 Tab 的主动事件统计（含 evt_earnings 维度）、宽表 daily_feature_base 的 evt_earnings 哑变量（2 行）
- **如何发现**：#10 收尾时主动向用户报告遗留影响面，用户确认清理
- **根因**：数据构成盘点确认——港股 75 条全是 `stock_financial_hk_report_em` 的 REPORT_DATE=**会计年度期末**（25 个去重日 × 3 重复，00700 从 2001 年起），非披露日；美股 1 条是 Yahoo"下次财报日"**未来日期**（2026-10-29）；A股 1 条日期语义虽正确（真实披露日）但财报体系已整体下线无分析价值。采集函数不停则删了会再采回
- **解决**：①停用三处采集入口——删 `collect_earnings_schedule` 整个函数（A股/港股源）、删 `collect_us_events` 的 next_earnings 段（美股源，保留分红）、`batch_collect_events` 仅采分红；②备份 DB 后 DELETE 77 行（175→98，仅剩分红）；③重建宽表（748→723 行，减少的 25 行正是 00700 财报去重日期撑出的无行情骨架行；evt_earnings 归零）；④重算模块3（主动事件维度 2→1）；⑤验证：清理脚本重跑零变化（幂等）、模块3/5/D3/每股/组合/D1 全部测试通过（回测期望值不变，证实 25 行无行情行本就被回测跳过）、浏览器 6 Tab 核验零财报残留零红错
- **预防**：**删脏数据必须连采集入口一起停**，否则下次点采集按钮原样采回（数据修复 = 存量清理 + 增量拦截，二者缺一不可）；宽表以"行情∪事件日期"为骨架时，无行情的事件日期会产生空骨架行，事件删除后要重建宽表并解释行数变化
