# 宏观经济日历数据源审计报告

> 审计日期：2026-09-02 | 起因：评估 dailyfx.com → IG 日历不可用后，审计 akshare 百度股市通经济日历作为宏观事件源
> 方法：5 轮渐进探测（可达性 → 失败定性 → 源码定位 → 绕过验证 → 锚点交叉验证），~25 个采样日期
> 脚本：audit_macro_calendar.py | 原始样本证据：macro_calendar_audit_sample.json

---

## 一、审计结论（总表）

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 数据内容质量 | ✅ 优 | 字段：时间(北京时间)/地区/事件/公布/预期/前值/重要性(1-2级)；覆盖美/中/欧/英/日/德等 14+ 地区 |
| 历史实际值填充 | ✅ 100% | 历史日中高重要性事件实际值填充率 100%（6 日采样），昨日 99% |
| NFP 锚定 | ✅ 2/2 可测日命中 | 首周五含"美国X月非农就业人口变动季调后" |
| 按日幂等性 | ✅ 一致 | 同日两次采集 28 vs 28 行完全一致 |
| 日期纯净性 | ✅ 通过 | 响应只含请求日期（首轮报告的"异常"为审计脚本类型比较 bug，已复核排除） |
| 数据洞 | ✅ 无 | 首轮 3 个"403 失败日"经自写客户端 7/7 全部取回（total 101~129）——**失败是 akshare bug，不是数据洞** |
| akshare 封装可靠性 | ❌ 不可直接用 | 两个 bug（见下），事件数>100 的日期大概率 403 |
| 自写客户端 | ✅ 可用 | 全请求带浏览器指纹 + 手动分页，7/7 失败日绕过成功 |
| 历史深度 | ✅ 覆盖回测窗口 | 2025-07-29 起全部可取（回测窗口 2025-07-29~2026-08-26 无缺口） |
| 时区语义 | ✅ 北京时间 | 美国非农/CPI 时刻 20:30（= 8:30 ET）、ISM 22:00（= 10:00 ET） |

## 二、akshare news_economic_baidu 的两个坑（踩坑库 #16）

1. **默认日期硬编码** `date: str = "20251126"`——不传日期拿到的是 2025-11-26 陈旧数据，必须显式传日期
2. **分页请求漏带浏览器指纹**：第一页请求带 `impersonate="chrome110"`，翻页请求（pn≥1）漏带 → 事件总数 >100 的日期第二页被 WAF 403。审计中曾误判为"周三数据洞"（7 个失败日恰逢周三，概率巧合 1/33649），读源码后定位真因

**绕过方案（已验证）**：自写薄客户端 = `_get_baidu_cookie()`（akshare 私有函数，两步取 BAIDUID/HMACCOOKIE）+ curl_cffi `impersonate="chrome110"` 全请求 + 按 total 手动分页。接口：`finance.pae.baidu.com/sapi/v1/financecalendar`，参数 `start_date/end_date/pn/rn/cate/finClientType`

## 三、意外收获：report_time = 正确的财报披露日源（S3 复活的钥匙）

同一 sapi 接口 `cate=report_time` 返回**个股财报发布日期**（区别于当年坑掉 S3 的"报告期"语义）：

- **覆盖 SH/SZ/HK/US 四个市场**（样例：香港交易所/洛阳钼业 HK、国泰海通 SH、DELL/PANW/MDT US）
- **锚点交叉验证 2/2 通过**：宁德时代 2025年年报 发布日=2026-03-10、浦发银行 发布日=2026-03-31，与东财预约披露表完全一致
- 历史深度 ≥ 2026-03（审计日回看），单日可含 152 条披露（2026-08-19）
- 注意：未来日期是预约排期（可能变更），回填用实际值、前向采集需日后校验
- 采集同样需自写客户端（该接口单日 total 可达 200，必翻页）

## 四、对项目的意义（两条可选路线，均未开工）

1. **宏观事件模块**（模块 4 的宏观版）：FOMC/CPI/非农等 → 市场级事件表（**不进 active_events**，无股票代码，需新表设计）→ 事件研究。可行，数据质量已验
2. **S3 财报策略复活**：用 report_time 重建 earnings 采集（幂等 + 披露日语义 + 名称映射校验）。协议层面合规：S3 删除时从未见过有效回测结果（数据全坏），重建后属"新数据上的预注册假设检验"，非依结果重选
3. 事件时刻 → 交易日的映射规则（沿用既有约束）：北京时间 20:30 的美国事件落在同一美国交易日、A/HK 效应记次日；盘中/盘后发布事件映射下一交易日

## 五、遗留注意

- 重要性格实际只有 1-2 两级（3 级从未出现），落地时 star=2 即"重点关注"
- 周日可能空响应（空 DataFrame 无列），采集代码需按"空=当日无事件"处理而非报错
- 请求节流：实测 3~8 秒间隔稳定；cookie 每次重新获取（约 2 次前置请求）
