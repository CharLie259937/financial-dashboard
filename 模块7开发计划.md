# 模块7开发计划：Forward Test 前向测试基础设施

> 优先级 P0（2026-09-03 路线图方向一）。依据：回测冻结协议规定"严格意义的样本外 = 2026-09 起的 forward test"，当前证据为零；S1d（oos -2.4%）与 RSI24（观察中 36 条）均等 forward 样本裁决。
> **状态：2026-09-03 全部交付完成（S1-S10 ✅），测试 37/37，浏览器验证通过。**

## 一、目标（四件套）

1. **每日自动管道**：行情补采 → 指数补采 → 指标重算 → 被动信号扫描 → 宽表重建 → 前向记录追加，单命令入口，幂等，可挂 Windows 计划任务
2. **前向追加式记录**：forward_trades / forward_equity / forward_meta 三表，确定性重放 + 完整性截断 + 追加守卫（已入库日期只校验不改写）
3. **观察名单裁决机制**：S1d / RSI24(S1e) 的转正/淘汰规则预注册写死在代码与 meta 中，不由人临时判断
4. **数据新鲜度监控**：每股行情相对其市场指数的落后交易日数，>2 交易日看板置顶红色告警

## 二、核心设计决策（2026-09-03 冻结）

### 2.1 前向窗口锚点
- `FORWARD_START = '2026-08-28'`：冻结回测批（run_date=2026-09-03）数据终点为 2026-08-27，前向窗口取其后首个交易日
- 前向信号口径：`trade_date >= FORWARD_START` 的触发；边界日前（≤08-27）的信号归冻结回测的 oos 段，跨界持仓不重复计算（接缝诚实披露：跨界信号只归 oos，前向净值从 1 重起）
- 跨界说明：若日后重跑 D2 批量回测（oos 终点会后移），两套记录允许重叠——前向记录以前向窗口常量为准，是前向时代的权威证据

### 2.2 复用 D1 引擎（口径完全一致）
- `_load_backtest_data()`（池过滤版）+ `_collect_triggers()` + `_run_strategy_bt()`（T+1 开盘成交、K=3 槽位、end_dropped 弃末端）
- 费率 `FORWARD_FEE = 0.001`（与 D2 呈现口径一致的中档费率）
- 覆盖全部 6 策略（S1a-S1e/S2）——裁决需要 S1d/S1e 数据，计算量可忽略
- B1 前向基准：`_buy_hold_baseline(fwd, '2026-08-27')` 各股在冻结窗后首个交易日开盘建仓；B2 同口径指数日收益合成

### 2.3 确定性重放 + 完整性截断 + 追加守卫
每次 forward step 全窗口重算（代码+冻结参数+历史数据不变 ⇒ 结果确定）：
- **完整性截断（2026-09-03 问题日志#1 修订，写入协议 amendment）**：
  - 个股净值（策略+B1）只追加到 `池内全部股票行情终局日的最小值`（读行情走池过滤）
  - B2 净值只追加到 `三市场指数终局日的最小值`
  - 卖出日 > 截断日的交易不入库（待后续运行）
  - 动机：美股未收盘时运行步进，当日 A/HK 已终局而美股缺行，ffill 值一旦入库，次日真实收盘到来必触发守卫误报且逐日累积
- 已入库日期：NAV 逐日比对（容差 1e-9），不一致记入 status 告警（数据修订可见化），**禁止 UPDATE**
- 越界遗留行（截断规则生效前误存）记入 status.stored_beyond_cap 可见化，提示人工清理，不自动删除
- 新日期：追加 equity 行；新交易按业务键 `UNIQUE(strategy_id, code, trigger_date)` 去重追加
- 指标口径：总收益/胜率/超额均取截断日口径（b1_total、capped_trades），与入库行严格一致

### 2.4 裁决规则（预注册，写入 forward_meta.protocol）
- 门槛：forward 完成交易数 ≥ 20 才可裁决；未满显示进度 n/20
- **S1d（KDJ金叉3日，oos -2.4%）**：`excess_vs_B1 ≤ 0 且 交易胜率 < 50%` → 淘汰建议；`excess_vs_B1 > 0 且 胜率 ≥ 50%` → 保留；否则继续观察
- **S1e（RSI24超卖10日，模块3状态=观察）**：同上阈值给出 forward 证据方向；**正式转正仍需时间分割验证（模块8 扩样本后执行）**，forward 仅作并行证据
- 规则一经写入不得依前向结果修改（与回测冻结协议同源）；协议同日修订仅限基础设施缺陷修复（amendment 字段留痕），裁决阈值从未改动

### 2.5 新鲜度口径
- 每股行情落后数 = 该股市场指数日历中 `> 个股最新行情日` 的交易日数（以指数为市场交易日历）
- `> 2 交易日` → 红色告警（看板量化分析页顶部横幅）；指数/宏观日历滞后仅展示不告警

## 三、表结构（实际实现）

```sql
CREATE TABLE IF NOT EXISTS forward_trades(
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  strategy_id TEXT NOT NULL, code TEXT, market TEXT,
  trigger_date TEXT, entry_date TEXT, entry_price REAL,
  exit_date TEXT, exit_price REAL,
  return_pct REAL, stat_ret REAL, gap_cost REAL,
  holding_days INTEGER, run_ts TEXT,
  UNIQUE(strategy_id, code, trigger_date)   -- 幂等业务键
);
CREATE TABLE IF NOT EXISTS forward_equity(
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  strategy_id TEXT NOT NULL, trade_date TEXT,
  nav REAL, run_ts TEXT,
  UNIQUE(strategy_id, trade_date)            -- 追加守卫锚点
);
CREATE TABLE IF NOT EXISTS forward_meta(
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  meta_key TEXT, result_json TEXT,
  run_date TEXT, generated_at TEXT
);
-- strategy_id ∈ {S1a..S1e, S2, B1, B2}; B1/B2 作为独立 strategy_id 行存 nav
-- （与初版设计差异：不设 b1_nav/b2_nav 旁列，B1/B2 与策略同表同键更简单，
--   看板读取统一走 get_forward_view().equity 字典）
-- forward_meta.meta_key ∈ {protocol(冻结,含amendment), status(最近步进快照),
--                          pipeline_last_run(最近管道报告)}
```

## 四、交付物清单

| # | 交付物 | 说明 |
|---|--------|------|
| 1 | quant_data.py 模块7节 | FORWARD_* 常量、create_forward_tables、run_forward_step（截断+守卫）、run_daily_pipeline、get_data_freshness、evaluate_watchlist、get_forward_view、get_pipeline_last_run |
| 2 | dashboard.py Tab 9「📡 前向跟踪」 | ①管道运行管理（st.status 实时日志）②新鲜度表 ③裁决面板 ④净值曲线 ⑤状态快照 ⑥交易明细 ⑦协议回读 七块 |
| 3 | 量化分析页顶部新鲜度横幅 | lag>2 红色告警 |
| 4 | daily_pipeline.py + run_daily_pipeline.bat | 单命令入口（bat 全 ASCII，防 GBK 解析错误；日志追加 daily_pipeline.log） |
| 5 | register_daily_task.bat | schtasks 注册每日 17:30 任务 FinancialDashboardPipeline（**仅创建脚本，注册需用户确认执行**） |
| 6 | test_module7.py | 10 组 37 项：建表幂等/确定性重放/追加幂等/协议冻结/唯一键/裁决结构/新鲜度/视图/管道报告 |
| 7 | 本文档 | 含问题日志节 |

## 五、执行步骤

- [x] S1 写本计划（本文档）
- [x] S2 quant_data.py：常量 + 三表 + run_forward_step（含守卫）
- [x] S3 quant_data.py：get_data_freshness + evaluate_watchlist + run_daily_pipeline + get_forward_view
- [x] S4 手动跑一次 forward step，核验前向窗口/净值/交易与 D1 引擎一致性
- [x] S5 dashboard.py：Tab 9 + 新鲜度横幅
- [x] S6 daily_pipeline.py + 两个 bat；管道全链路手动执行一次（两次全绿）
- [x] S7 test_module7.py 全部通过（37/37，幂等重跑零新增）
- [x] S8 浏览器逐项验证 Tab 9 与横幅（含 Tab 6 回归）
- [x] S9 双目录同步 + SKILL 更新备份 + 项目记忆
- [x] S10 问题日志（4 条）

## 六、问题日志

（每次遇到错误并修复，按五要素追加：现象 / 如何发现 / 根因 / 解决 / 预防）

### #1 前向净值以未终局数据入库（ffill 提前入库 → 守卫误报）
- **现象**：首次完整运行管道后守卫报 2 条 NAV 不一致（B1/B2 @ 2026-09-03，stored 0.9874/0.9880 vs 新算 0.9835/0.9870）
- **如何发现**：daily_pipeline.py 首跑 forward 步骤日志"守卫告警: 2 条"；读 forward_meta.status 定位到 09-03 的 B1/B2 两行
- **根因**：两层。表层：22:14 用 _verify_m7.py 验证时直接调 run_forward_step，当时 00700 的 09-03 行情与基准指数 09-03 行尚未采集，B1/B2 当日 NAV 以 09-02 前向填充值入库，管道采到真实收盘后重算必然不一致。深层（设计缺陷）：三市场时差下美股收盘最晚（北京时间次日凌晨），17:30 计划任务运行时当日美股行必然缺失，ffill 值入库后次日真实数据到来必触发守卫误报且逐日累积
- **解决**：① run_forward_step 增加完整性截断（见 2.3）；② 人工修复：备份 quant_data_backup_20260903_pre_cap_repair.db 后删除 8 条 09-03 净值行，重跑步进按截断日 09-02 对齐（明日美股收盘后 09-03 行将以终局数据补入）；③ 协议 JSON 追加 amendment 字段留痕（裁决规则未变）
- **预防**：前向步进永远作为管道最后一步（采集先行）；任意时点手动运行均受截断保护；status 增加 stored_beyond_cap 字段可见化越界遗留行

### #2 Tab 9 面板空白（st.stop() 静默中断脚本）
- **现象**：浏览器验证「📡 前向跟踪」标签可选中但面板完全空白，页面上无任何报错
- **如何发现**：浏览器自动化逐项核对 Tab 9 七个区块，DOM 检查 tabpanel 内只有空的 stVerticalBlock；服务端日志无 traceback
- **根因**：Tab 6（信号效果）块内的 `st.stop()`（会话无 m3_result 时触发）——Streamlit 的 StopException 被静默吞掉不显示任何错误。历史上 Tab 6 是最后一个 with 块所以无害；Tab 9 插到其后后脚本在 Tab 6 处中断，Tab 9 永不渲染。Tab 7 代码早有"任何分支都不使用 st.stop()"的纪律注释，Tab 6 是漏网之鱼
- **解决**：`st.stop()` 改为 `else:` 分支（后续 405 行整体缩进 +4），无 m3_result 时显示提示信息后正常结束标签块
- **预防**：全页面禁用 st.stop()（tab7 注释已立此纪律，本次扩展到全部标签）；新增标签页必须在浏览器验证面板非空；教训写入 SKILL 踩坑库

### #3 测试跑在开发目录旧库上大面积失败
- **现象**：test_module7.py 首跑 T2 报"前向窗口无交易日数据"、T8 直接 traceback "no such table: macro_events"
- **如何发现**：测试输出（T1-T4 失败、T8 崩溃）
- **根因**：开发目录的 quant_data.db 是旧副本（行情至 08-27、无 macro_events 表、forward 三表为空）；权威库在 Desktop 运行目录（双目录约定 .db 以运行目录为准）。测试脚本以自身位置定位库文件，在开发目录运行即读旧库
- **解决**：同步代码到 Desktop 后从运行目录执行，37/37 通过；对比两库确认 Desktop 为准（行情至 09-03、macro_events 在、forward_equity 40 行）
- **预防**：测试一律在运行目录执行（test_module7.py 头部已注明运行位置）；开发目录只放代码不放数据状态

### #4 探测脚本列名凭记忆写错
- **现象**：查 daily_quotes 的 `pct_change` 列报 no such column
- **如何发现**：排查 #1 时运行临时探测脚本直接抛错
- **根因**：实际列名为 `change_pct`，凭记忆写成 `pct_change`
- **解决**：`PRAGMA table_info` 确认后改用正确列名
- **预防**：查不熟悉的表先 PRAGMA 确认列名再写 SQL

## 七、后续维护备忘

- **明日（美股 09-03 收盘后）首次自然补录**：管道运行后 09-03 净值行将以全部终局数据补入（8 序列各 1 行），同时前向窗口进入正常节奏
- **计划任务注册**：register_daily_task.bat 需用户手动执行确认（schtasks 每日 17:30）；卸载命令在脚本头注释
- **裁决时间预期**：S1d 持有 3 日、S1e 持有 10 日，20 笔门槛按当前信号频率估算需 1-3 个月样本积累
- **观察 end_dropped**：前向窗口初期（<10 个交易日）持有期末满的交易不入库属正常，窗口拉长后自然消化
